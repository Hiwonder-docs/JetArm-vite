import{_ as c,o as d,c as h,ah as a,j as e,a as s}from"./chunks/framework.3lAyvTsY.js";const _="/projects/JetArm/en/jetarm-orin-nano/assets/image1.DKbN2SrO.webp",g="/projects/JetArm/en/jetarm-orin-nano/assets/image2.CzKk3Nrd.webp",m="/projects/JetArm/en/jetarm-orin-nano/assets/image3.Cyso2oaH.webp",u="/projects/JetArm/en/jetarm-orin-nano/assets/image4.Dh3yZtFF.webp",f="/projects/JetArm/en/jetarm-orin-nano/assets/image5.CcQcayFp.webp",b="/projects/JetArm/en/jetarm-orin-nano/assets/image6.B3jp-pk5.webp",v="/projects/JetArm/en/jetarm-orin-nano/assets/image13.BZnXUCWR.webp",y="/projects/JetArm/en/jetarm-orin-nano/assets/image14.C6AKK1N-.webp",T="/projects/JetArm/en/jetarm-orin-nano/assets/image15.CGXscDyb.webp",w="/projects/JetArm/en/jetarm-orin-nano/assets/image16.Dfp_9_0j.webp",k="/projects/JetArm/en/jetarm-orin-nano/assets/image17.CqlHM2j0.webp",x="/projects/JetArm/en/jetarm-orin-nano/assets/image18._MWPfkop.webp",C="/projects/JetArm/en/jetarm-orin-nano/assets/image19.2iwKTReW.webp",S="/projects/JetArm/en/jetarm-orin-nano/assets/image20.D_-wE6L7.webp",A="/projects/JetArm/en/jetarm-orin-nano/assets/image21.BUxSvaKi.webp",I="/projects/JetArm/en/jetarm-orin-nano/assets/image22.BJ5OYYdn.webp",P="/projects/JetArm/en/jetarm-orin-nano/assets/image12.cMMNUvrM.webp",R="/projects/JetArm/en/jetarm-orin-nano/assets/image13.J7dOsHYI.webp",q="/projects/JetArm/en/jetarm-orin-nano/assets/image14.Cx9fwPai.webp",n="/projects/JetArm/en/jetarm-orin-nano/assets/image26.BX_aOvvv.webp",t="/projects/JetArm/en/jetarm-orin-nano/assets/image4.BB8nzSAp.webp",p="/projects/JetArm/en/jetarm-orin-nano/assets/image16.DODYWcge.webp",N="/projects/JetArm/en/jetarm-orin-nano/assets/image32.esjDXU6H.webp",o="/projects/JetArm/en/jetarm-orin-nano/assets/image33.DPoaR8I4.webp",E="/projects/JetArm/en/jetarm-orin-nano/assets/image35.BOIvyuhU.webp",z="/projects/JetArm/en/jetarm-orin-nano/assets/image74.CMPU4Zsh.webp",l="/projects/JetArm/en/jetarm-orin-nano/assets/image82.ZjXH4rEA.webp",j="/projects/JetArm/en/jetarm-orin-nano/assets/image83.Brd4wJA1.webp",D="/projects/JetArm/en/jetarm-orin-nano/assets/image84.qWQVRrZa.webp",V="/projects/JetArm/en/jetarm-orin-nano/assets/image85.B0bQ6_iQ.webp",O="/projects/JetArm/en/jetarm-orin-nano/assets/image86.BvorfptA.webp",r="/projects/JetArm/en/jetarm-orin-nano/assets/image87.DCrBUM52.webp",M="/projects/JetArm/en/jetarm-orin-nano/assets/image80.COaIagOZ.webp",B="/projects/JetArm/en/jetarm-orin-nano/assets/image108.B1mP2MOP.webp",G="/projects/JetArm/en/jetarm-orin-nano/assets/image109.DgYjRvXl.webp",L="/projects/JetArm/en/jetarm-orin-nano/assets/image110.CrutFput.webp",F="/projects/JetArm/en/jetarm-orin-nano/assets/image111.CPSPS75c.webp",Z=JSON.parse('{"title":"10. ROS1-Robot Arm + Sliding Rail Course","description":"","frontmatter":{},"headers":[],"relativePath":"docs/10.ROS1_Robot_Arm+Sliding_Rail_Course.md","filePath":"docs/10.ROS1_Robot_Arm+Sliding_Rail_Course.md"}'),K={name:"docs/10.ROS1_Robot_Arm+Sliding_Rail_Course.md"};function U(J,i,W,Y,X,H){return d(),h("div",null,[...i[0]||(i[0]=[a('<h1 id="_10-ros1-robot-arm-sliding-rail-course" tabindex="-1">10. ROS1-Robot Arm + Sliding Rail Course <a class="header-anchor" href="#_10-ros1-robot-arm-sliding-rail-course" aria-label="Permalink to &quot;10. ROS1-Robot Arm + Sliding Rail Course&quot;">​</a></h1><h2 id="_10-1-sliding-rail-installation" tabindex="-1">10.1 Sliding Rail Installation <a class="header-anchor" href="#_10-1-sliding-rail-installation" aria-label="Permalink to &quot;10.1 Sliding Rail Installation&quot;">​</a></h2><p>You can refer to the instructional videos in the same directory for installing the slide rail and robotic arm: &#39;<a href="https://youtu.be/kmQXTRHfAfU" target="_blank" rel="noreferrer"><strong>Install Tank Belt to Slider</strong></a>&#39; and &#39;<a href="https://youtu.be/rqszXsAqTRI" target="_blank" rel="noreferrer"><strong>Install JetArm to slider</strong></a>&#39; Below is a diagram illustrating the installation of the sliding rail and robotic arm:</p><table class="docutils-nobg" border="1"><colgroup><col></colgroup><tbody><tr><td><h2 id="step-1-install-cable-carrier" class="unnumbered">Step 1: Install cable carrier</h2><p>Use two M4*6 screws to attach the cable carrier to the sliding rail.</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+_+'"></p></td></tr></tbody></table><table class="docutils-nobg" border="1"><colgroup><col></colgroup><tbody><tr><td><h2 id="step-2-install-the-motor-mounting-cover" class="unnumbered">Step 2: Install the motor mounting cover</h2><p>Use two M3*6 screws to attach the motor mounting cover to the sliding rail.</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+g+'"></p></td></tr><tr><td><h2 id="step-3-install-the-sliding-rail-base-plate" class="unnumbered">Step 3: Install the sliding rail base plate</h2><p>Use four M5*12 screws and four 5.27*4 nylon standoff to secure the sliding rail base plate to the slide rail.</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+m+'"></p></td></tr><tr><td><h2 id="step-4-install-the-sliding-rail-adapter-bracket" class="unnumbered">Step 4: Install the sliding rail adapter bracket</h2><p>Use two M4*8 round-head screws to attach the sliding rail adapter bracket to the JetArm base plate. Then, use two M3*6 screws to install two M3*15 dual-hole brass standoffs on the JetArm adapter plate</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+u+'"></p></td></tr><tr><td><h2 id="step-3-install-the-sliding-rail-base-plate" class="unnumbered">Step 5: Install the robotic arm</h2><p>Use four M3*10 screws to assemble the robotic arm onto the sliding rail base plate. (Note: You will need to remove the M3*6 screws that are originally installed on the robotic arm first.)</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+f+'"></p></td></tr><tr><td><h2 id="step-3-install-the-sliding-rail-base-plate" class="unnumbered">Step 6: Attach the cable carrier to the robotic arm</h2><p>Use two M3*6 screws to assemble the cable carrier onto the JetArm robotic arm.</p><p><img loading="lazy" height="600" width="1200" class="common_img" src="'+b+'"></p></td></tr><tr><td><h2 id="step-3-install-the-sliding-rail-base-plate" class="unnumbered">Step 7: Completed assembly and wiring instructions</h2><p>The completed assembly is shown in the diagram below:</p><p><img loading="lazy" height="808" width="1267" class="common_img" src="'+v+'"></p></td></tr><tr><td><p>The 3-pin wire of the sliding rail connects to the 3-pin terminal on the STM32 expansion board for power supply, while the 4-pin wire connects to the I2C interface on the Jetson Nano expansion board, as shown in the diagram below:</p><p><img loading="lazy" height="639" width="1292" class="common_img" src="'+y+'"></p></td></tr></tbody></table><h2 id="_10-2-position-calibration" tabindex="-1">10.2 Position Calibration <a class="header-anchor" href="#_10-2-position-calibration" aria-label="Permalink to &quot;10.2 Position Calibration&quot;">​</a></h2><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p></p><ul><li><p>This section pertains to position calibration in the sliding rail configuration. Please install the robotic arm on the sliding rail before proceeding with this operation.</p></li><li><p>After using the sliding rail, if you need to utilize it with a map, you must recalibrate the position on the map. Please refer to <a href="./1.Getting_Ready.html#position-calibration">1. Getting Ready( JetArm User Manual)-&gt;1.4.5 Position Calibration</a> for guidance.</p></li></ul></div><p>Position calibration refers to the process of converting the coordinates from the camera&#39;s returned image to real-world coordinates. Through position calibration, the actual distance between the block and the robotic arm can be determined, allowing for improved performance in sorting and other related tasks.</p><p>(1) After successfully connecting the robotic arm to the app, tap on the robot arm icon and select &#39;<strong>Position Calibration</strong>&#39; from the mode selection interface.</p><img loading="lazy" height="595" width="1266" class="common_img" src="'+T+'"><img loading="lazy" height="452" width="981" class="common_img" src="'+w+'"><p>(2) When using the app for the first time, the image on the right may take a few seconds to appear, so please wait a moment. If the image does not appear after a long time, return to the mode selection page and re-enter.</p><img loading="lazy" height="618" width="1266" class="common_img" src="'+k+'"><p>(3) Place the AprilTag label for ID1 directly beneath the depth camera, ensuring that the text &#39;<strong>TAG36H11-1</strong>&#39; faces the left side of the robotic arm, as shown in the diagram below. Incorrect placement of the label may lead to an erroneous ROI (Region of Interest) for the robotic arm.</p><p class="common_img" style="text-align:center;"><img loading="lazy" height="636" width="555" src="'+x+'" style="width:420px;"><img loading="lazy" height="660" width="629" src="'+C+'" style="width:420px;"></p><p>(4) Press the &#39;<strong>Start Position Calibration</strong>&#39; button. When you see the five black dots align with the five blue dots on the AprilTag label, and the message &#39;Calibration finished!&#39; appears on the screen, the calibration process is complete.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p></p><p>Calibration may take some time, so please be patient.</p></div><img loading="lazy" height="616" width="1266" class="common_img" src="'+S+'"><p>(5) After the calibration is complete, exit the calibration mode and switch to the <strong>&#39;Object Sorting&#39;</strong> mode to check if the ROI (Region of Interest) is functioning properly. The examples of correct and incorrect ROI areas are illustrated in the diagram below:</p><img loading="lazy" height="587" width="1267" class="common_img" src="'+A+'"><img loading="lazy" height="579" width="1267" class="common_img" src="'+I+'"><h2 id="_10-3-wireless-handle-control" tabindex="-1">10.3 Wireless Handle Control <a class="header-anchor" href="#_10-3-wireless-handle-control" aria-label="Permalink to &quot;10.3 Wireless Handle Control&quot;">​</a></h2><h3 id="_10-3-1-preparation" tabindex="-1">10.3.1 Preparation <a class="header-anchor" href="#_10-3-1-preparation" aria-label="Permalink to &quot;10.3.1 Preparation&quot;">​</a></h3><p>(1) Before turning on the device, please ensure that the controller receiver is plugged in. If it is already connected, you can ignore this step (the USB controller receiver is pre-installed in the robot at the factory).</p><p>(2) When inserting the battery, be sure to distinguish between the positive and negative terminals.</p><img loading="lazy" height="482" width="1288" class="common_img" src="'+P+'"><p>(3) Each time the robot is powered on, the app auto-start service will be activated (this includes the controller control service). If you haven&#39;t disabled this service, no further action is needed; you can connect and control the robot directly.</p><p>(4) Due to potential interference from multiple controllers, it is recommended not to use this feature if multiple robots are operating in the same area to avoid accidental connections and control issues.</p><p>(5) After turning on the controller, if it does not connect to the robot within 30 seconds, or if it remains unused for 5 minutes after connecting, the controller will automatically enter sleep mode. To wake the controller and exit sleep mode, simply press the &#39;<strong>START</strong>&#39; button.</p><h3 id="_10-3-2-device-connection" tabindex="-1">10.3.2 Device Connection <a class="header-anchor" href="#_10-3-2-device-connection" aria-label="Permalink to &quot;10.3.2 Device Connection&quot;">​</a></h3><p>(1) After the robot has powered on, slide the switch on the controller to the &quot;<strong>ON</strong>&quot; position. At this point, both LED lights on the controller (red and green) will flash simultaneously.</p><p>(2) Wait a few seconds for the robot and controller to pair automatically. When pairing is successful, the green LED will remain lit, and the red LED will turn off.</p><img loading="lazy" height="552" width="1070" class="common_img" src="'+R+'"><h3 id="_10-3-3-introduction-to-control-modes" tabindex="-1">10.3.3 Introduction to Control Modes <a class="header-anchor" href="#_10-3-3-introduction-to-control-modes" aria-label="Permalink to &quot;10.3.3 Introduction to Control Modes&quot;">​</a></h3><p>The controller operates in two modes: Coordinate Mode and Single Servo Mode. After a successful connection, the default mode is Coordinate Mode.</p><p>(1) Single Servo Mode：Use the buttons on the controller to control the rotation of a single servo on the robotic arm in both clockwise and counterclockwise directions.</p><img loading="lazy" height="953" width="1268" class="common_img" src="'+q+'"><p>(2) Coordinate Mode: Using the buttons on the controller, you can control the robotic arm to move as a whole along a three-axis coordinate system (X, Y, Z) while also adjusting the deflection angle.</p><img loading="lazy" height="673" width="956" class="common_img" src="'+n+'"><p>Switching Between Modes: To switch between the two modes, press the &quot;<strong>SELECT</strong>&quot; and &quot;<strong>START</strong>&quot; buttons. You will hear a confirmation sound to indicate a successful switch.</p><p>① Single Servo Mode to Coordinate Mode: <strong>Two beeps</strong></p><p>② Coordinate Mode to Single Servo Mode: <strong>One beep</strong></p><h3 id="_10-3-4-remote-control-button-instructions" tabindex="-1">10.3.4 Remote Control Button Instructions <a class="header-anchor" href="#_10-3-4-remote-control-button-instructions" aria-label="Permalink to &quot;10.3.4 Remote Control Button Instructions&quot;">​</a></h3><ul><li><strong>Single Servo Mode Button Instructions</strong></li></ul><p>The table below describes the button functions for Single Servo Mode:</p><table tabindex="0"><thead><tr><th style="text-align:center;"><strong>Button</strong></th><th style="text-align:center;"><strong>Function（From the First-Person Perspective of the Robotic Arm）</strong></th></tr></thead><tbody><tr><td style="text-align:center;">START</td><td style="text-align:center;">Reset robot arm</td></tr><tr><td style="text-align:center;">SELECT+START</td><td style="text-align:center;">Switch Control Mode (Single Servo/Coordinate)</td></tr><tr><td style="text-align:center;">UP / ↑</td><td style="text-align:center;">Raise Servo 2</td></tr><tr><td style="text-align:center;">DOWN / ↓</td><td style="text-align:center;">Lower Servo 2</td></tr><tr><td style="text-align:center;">LEFT / ←</td><td style="text-align:center;">Turn Servo 1 Left</td></tr><tr><td style="text-align:center;">RIGHT / →</td><td style="text-align:center;">Turn Servo 1 Right</td></tr><tr><td style="text-align:center;">Y</td><td style="text-align:center;">Close Gripper (Servo 10)</td></tr><tr><td style="text-align:center;">A</td><td style="text-align:center;">Open Gripper (Servo 10)</td></tr><tr><td style="text-align:center;">B</td><td style="text-align:center;">Turn Servo 5 Right (Gripper Turns Right)</td></tr><tr><td style="text-align:center;">X</td><td style="text-align:center;">Turn Servo 5 Left (Gripper Turns Left)</td></tr><tr><td style="text-align:center;">L1</td><td style="text-align:center;">Raise Servo 3</td></tr><tr><td style="text-align:center;">L2</td><td style="text-align:center;">Lower Servo 3</td></tr><tr><td style="text-align:center;">R1</td><td style="text-align:center;">Raise Servo 4</td></tr><tr><td style="text-align:center;">R2</td><td style="text-align:center;">Lower Servo 4</td></tr><tr><td style="text-align:center;">LEFT</td><td style="text-align:center;">Move Slide Rail Left</td></tr><tr><td style="text-align:center;">RIGHT</td><td style="text-align:center;">Move Slide Rail Right</td></tr></tbody></table><ul><li><strong>Coordinate Mode Button Instructions</strong></li></ul><p>The table below describes the button functions for Coordinate Mode:</p><table tabindex="0"><thead><tr><th style="text-align:center;"><strong>Button</strong></th><th style="text-align:center;"><strong>Function（From the First-Person Perspective of the Robotic Arm）</strong></th></tr></thead><tbody><tr><td style="text-align:center;">START</td><td style="text-align:center;">Reset Robot Arm</td></tr><tr><td style="text-align:center;">SELECT+START</td><td style="text-align:center;">Switch Control Mode (Single Servo/Coordinate)</td></tr><tr><td style="text-align:center;">UP / ↑</td><td style="text-align:center;">Move Robot Arm in Positive X Direction (Forward)</td></tr><tr><td style="text-align:center;">DOWN / ↓</td><td style="text-align:center;">Move Robot Arm in Negative X Direction (Backward)</td></tr><tr><td style="text-align:center;">LEFT / ←</td><td style="text-align:center;">Move Robot Arm in Positive Y Direction (Left)</td></tr><tr><td style="text-align:center;">RIGHT / →</td><td style="text-align:center;">Move Robot Arm in Negative Y Direction (Right)</td></tr><tr><td style="text-align:center;">Y</td><td style="text-align:center;">Close Gripper (Servo 10)</td></tr><tr><td style="text-align:center;">A</td><td style="text-align:center;">Open Gripper (Servo 10)</td></tr><tr><td style="text-align:center;">B</td><td style="text-align:center;">Turn Servo 5 Right (Gripper Turns Right)</td></tr><tr><td style="text-align:center;">X</td><td style="text-align:center;">Turn Servo 5 Left (Gripper Turns Left)</td></tr><tr><td style="text-align:center;">L1</td><td style="text-align:center;">Move Robot Arm in Positive Z Direction (Upward)</td></tr><tr><td style="text-align:center;">L2</td><td style="text-align:center;">Move Robot Arm in Negative Z Direction (Downward)</td></tr><tr><td style="text-align:center;">R1</td><td style="text-align:center;">Increase Gripper Pitch Angle</td></tr><tr><td style="text-align:center;">R2</td><td style="text-align:center;">Decrease Gripper Pitch Angle</td></tr><tr><td style="text-align:center;">LEFT joystick</td><td style="text-align:center;">Move Slide Rail Left</td></tr><tr><td style="text-align:center;">RIGHT joystick</td><td style="text-align:center;">Move Slide Rail Right</td></tr></tbody></table><h2 id="_10-4-waste-sorting" tabindex="-1">10.4 Waste Sorting <a class="header-anchor" href="#_10-4-waste-sorting" aria-label="Permalink to &quot;10.4 Waste Sorting&quot;">​</a></h2><h3 id="_10-4-1-program-logic" tabindex="-1">10.4.1 Program Logic <a class="header-anchor" href="#_10-4-1-program-logic" aria-label="Permalink to &quot;10.4.1 Program Logic&quot;">​</a></h3><p>In this section, we will classify waste wood blocks using a trained YOLOv5 model recognized by the camera. The robot arm will then utilize inverse kinematics to grasp the identified waste wood blocks and sort them into the corresponding waste bins.</p><p><strong>Step 1: Recognition and Classification</strong></p><p>First, we subscribe to the real-time image data published by the camera node and convert it to Numpy format. After the conversion, we input the image into the YOLOv5 network for scaling, transposing, and array expansion to obtain processing results.</p><p>Next, we convert the coordinates of the detected waste card images back to the original image coordinates. After converting the coordinates, we classify the waste according to the recognized names and predefined rules, retrieving the corresponding waste category names. Finally, we outline the waste items and display their names along with the recognition confidence levels.</p><p><strong>Step: Grasp and Place</strong></p><p>To ensure result reliability, repeated recognition of the object is required. The card with the highest confidence level in the image is selected as the first sorting target. Multiple recognition cycles are performed to confirm the object with high reliability.</p><p>Once the target is confirmed, the robotic arm&#39;s sorting and transport process is initiated, converting the target card&#39;s pixel coordinates to world coordinates. The robotic arm then moves to the location of the waste card, grasps the waste block, and, based on the waste category, moves along the sliding rail to the designated placement location for that category, where it releases the waste card, completing the waste sorting task.</p><h3 id="_10-4-2-start-and-close-the-game" tabindex="-1">10.4.2 Start and Close the Game <a class="header-anchor" href="#_10-4-2-start-and-close-the-game" aria-label="Permalink to &quot;10.4.2 Start and Close the Game&quot;">​</a></h3><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p></p><p>The input command should be case sensitive, and keywords can be complemented using Tab key.</p></div><p>(1) Access the robot system desktop using the NoMachine.</p><p>(2) Double-click <img loading="lazy" height="71" width="76" src="'+t+'"> to open the command line, then enter the following command and press Enter to stop the auto-start service:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.stop_ros.sh</span></span></code></pre></div><p>(3) Enter the command below to start the waste classification program and press Enter:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>roslaunch stepper waste_classification_stepper.launch</span></span></code></pre></div><p>(4) To stop the program, press &quot;<strong>Ctrl+C</strong>&quot;. If it does not close successfully, repeat this action multiple times.</p><p>(5) After completing the activity, the app service needs to be started <img loading="lazy" height="65" width="77" src="'+p+'"> (failing to start it may affect subsequent app activities). Enter the following command in the terminal and press Enter to start the app service:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo systemctl start start_app_node.service</span></span></code></pre></div><p>(6) Once the app service has started, the robotic arm will return to its initial position, and the buzzer will beep once.</p><h3 id="_10-4-3-program-outcome" tabindex="-1">10.4.3 Program Outcome <a class="header-anchor" href="#_10-4-3-program-outcome" aria-label="Permalink to &quot;10.4.3 Program Outcome&quot;">​</a></h3><p>Once the activity begins, the camera detects waste cards and displays their names on the screen. Each waste category is highlighted with a different color rectangle: hazardous waste in red, recyclable waste in blue, kitchen waste in green, and other waste in gray.</p><p>The robotic arm then grasps the waste blocks and sorts them into their designated locations. In the image below, different colored bins are used as props for each category.</p><img loading="lazy" height="701" width="1068" class="common_img" src="'+N+'"><table tabindex="0"><thead><tr><th style="text-align:center;"><strong>Waste Category</strong></th><th style="text-align:center;"><strong>Cards</strong></th></tr></thead><tbody><tr><td style="text-align:center;">hazardous_waste</td><td style="text-align:center;">Storage Battery, Marker, Oral Liquid Bottle</td></tr><tr><td style="text-align:center;">recyclable_waste</td><td style="text-align:center;">Plastic Bottle, Umbrella, Toothbrush</td></tr><tr><td style="text-align:center;">food_waste</td><td style="text-align:center;">Banana Peel, Ketchup, Broken Bones</td></tr><tr><td style="text-align:center;">residual_waste</td><td style="text-align:center;">Cigarette End, Plate, Disposable Chopsticks</td></tr></tbody></table><p>If the grasping performance is unsatisfactory, please refer to &quot;<a href="https://docs.hiwonder.com/projects/JetArm/en/latest/docs/1.Getting_Ready.html#position-calibration" target="_blank" rel="noreferrer"><strong>1. Getting Ready(JetArm User Manual)-&gt;1.4.5 Position Calibration</strong></a>&quot; for guidance. Here, you can only adjust the X and Y coordinate offsets during grasping. If you have used it with a sliding rail and plan to switch to map-based operations, you will need to readjust the coordinates on the map. For adjustments to the placement coordinates, please refer to <a href="#anchor_10_4_5">10.4.5 Function Extensions</a>.</p><h3 id="_10-4-4-package-structure-and-program-analysis" tabindex="-1">10.4.4 Package Structure and Program Analysis <a class="header-anchor" href="#_10-4-4-package-structure-and-program-analysis" aria-label="Permalink to &quot;10.4.4 Package Structure and Program Analysis&quot;">​</a></h3><ul><li><strong>Package Structure Analysis</strong></li></ul><p>When using the waste classification feature, the functionality package for the recognition program is called. Its file structure is shown in the diagram below:</p><img loading="lazy" height="254" width="264" class="common_img" src="'+o+'"><p><code>launch folder</code>: This folder contains the launch file for starting the waste classification program, which is used to activate the waste classification functionality.</p><p><code>scripts folder</code>: This folder contains the source code files for the waste classification program, which define the logic functions for waste classification.</p><p><code>CMakeLists.txt</code>:This file is used for compiling the package and managing related dependencies, including relevant format information needed after starting the ROS service.</p><p><code>package.xml</code>: This is the description file for the package, which outlines the details related to the current package, such as build tools and version numbers.</p><ul><li><strong>Program Analysis</strong></li></ul><p>(1) Launch File Analysis</p><p>The Launch file is saved in : <a href="../_static/source_code/10/stepper_launch.zip">/home/ubuntu/jetarm/src/stepper/launch/waste_classification_stepper.launch</a></p><p>During the execution of the functionality, the launch file for the current package (<strong>waste_classification_stepper.launch</strong>) will be activated. The contents are shown in the diagram below:</p>',87),e("p",{"lineno-start":"1"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;launch&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_type&quot; default=&quot;$(optenv CAMERA_TYPE GEMINI)&quot;/&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    &lt;!-- 根据使用的相机设置相应参数(set corresponding parameters based on the used camera) --&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;source_image_topic&quot; if=&quot;$(eval camera_type==&#39;GEMINI&#39;)&quot; default=&quot;/rgbd_cam/color/image_rect_color&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_info_topic&quot; if=&quot;$(eval camera_type==&#39;GEMINI&#39;)&quot; default=&quot;/rgbd_cam/color/camera_info&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;source_image_topic&quot; if=&quot;$(eval camera_type==&#39;USB_CAM&#39;)&quot; default=&quot;/usb_cam/image_rect_color&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_info_topic&quot; if=&quot;$(eval camera_type==&#39;USB_CAM&#39;)&quot; default=&quot;/usb_cam/camera_info&quot; /&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        &lt;include file=&quot;$(find jetarm_bringup)/launch/base.launch&quot;/&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        &lt;node name=&quot;waste_classification&quot; pkg=&quot;stepper&quot; type=&quot;waste_classification_stepper.py&quot; output=&quot;screen&quot; respawn=&quot;true&quot;&gt;</span></span>
<span class="line"><span>                &lt;param name=&quot;source_image_topic&quot; value=&quot;$(arg source_image_topic)&quot; /&gt;</span></span>
<span class="line"><span>                &lt;param name=&quot;camera_info_topic&quot; value=&quot;$(arg camera_info_topic)&quot; /&gt;</span></span>
<span class="line"><span>        &lt;/node&gt;</span></span>
<span class="line"><span>&lt;/launch&gt;</span></span></code></pre></div><p>From the diagram above, it is evident that it is necessary to check which type of camera is mounted on the robotic arm (either GEMINI or USB monocular camera). By obtaining the camera type in the current working environment, the corresponding topic information will be modified for subsequent recognition.</p><p>The base.launch file below is used to initialize the current state of the robotic arm. Within this base.launch file, nodes are published that control the joint movements of the robotic arm.</p><p>Finally, the waste classification functionality is executed by calling the source file <code>waste_classification_stepper.py</code>.</p><p>(2) Source Code Analysis</p><p>The source code for the program is located at: <a href="../_static/source_code/10/stepper_swiper.zip">jetarm/src/stepper/scripts/waste_classification_stepper.py</a></p><p>Based on the functional requirements, the logical flowchart of the program has been designed, as shown in the diagram below:</p><img loading="lazy" height="258" width="986" class="common_img" src="`+E+'"><p>From the diagram, the main logic flow of the program involves setting up the object recognition model, obtaining the camera intrinsic parameters, and then processing and recognizing the images and models. The calculated object coordinates are sent to the robotic arm, enabling it to grasp and place the waste blocks.</p><p>① Importing Relevant Application Libraries</p>',10),e("p",{"lineno-start":"3"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import os</span></span>
<span class="line"><span>import cv2</span></span>
<span class="line"><span>import rospy</span></span>
<span class="line"><span>import queue</span></span>
<span class="line"><span>import numpy as np</span></span>
<span class="line"><span>import threading</span></span>
<span class="line"><span>from vision_utils import fps, xyz_quat_to_mat, xyz_euler_to_mat, pixels_to_world, box_center, mat_to_xyz_euler, distance, extristric_plane_shift</span></span>
<span class="line"><span>from sensor_msgs.msg import Image as RosImage, CameraInfo</span></span>
<span class="line"><span>from std_srvs.srv import Trigger, TriggerRequest, TriggerResponse</span></span>
<span class="line"><span>from std_srvs.srv import SetBool, SetBoolRequest, SetBoolResponse</span></span>
<span class="line"><span></span></span>
<span class="line"><span>from std_srvs.srv import Empty</span></span>
<span class="line"><span></span></span>
<span class="line"><span>from yolov5_onnx import YOLOV5</span></span>
<span class="line"><span>from hiwonder_interfaces.srv import GetRobotPose</span></span>
<span class="line"><span>from hiwonder_interfaces.msg import MoveAction, MoveGoal, MultiRawIdPosDur</span></span>
<span class="line"><span>from jetarm_sdk import bus_servo_control</span></span>
<span class="line"><span>import actions</span></span>
<span class="line"><span>import actionlib</span></span></code></pre></div><p><code>cv2</code>: Used for OpenCV image processing</p><p><code>rospy</code>: Used for ROS communication</p><p><code>numpy</code>: Used for array operations</p><p><code>queue</code>, <code>threading</code>: Used for multithreading</p><p>Import various functional modules required for coordinate transformation calculations from vision_utils</p><p>Import message types from sensor_msgs.msg</p><p>Import service types from std_srvs.srv</p><p>Import the yolov5_trt model</p><p>Import the corresponding message and service types from hiwonder_interfaces</p><p>Import action groups from actions</p><p>Import the communication library actionlib</p>`,12),e("p",{"lineno-start":"23"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import stepper as Stepper</span></span></code></pre></div><p>Stepper is used to control the rotation of stepper motors</p><p>② Waste Model Configuration</p>',3),e("p",{"lineno-start":"44"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRT_NUM_CLASSES = 13</span></span>
<span class="line"><span>TRT_CLASS_NAMES = ( &#39;Banana Peel&#39;,&#39;Broken Bones&#39;, &#39;Cigarette End&#39;, &#39;Disposable Chopsticks&#39;,</span></span>
<span class="line"><span>                   &#39;Ketchup&#39;, &#39;Marker&#39;, &#39;Oral Liquid Bottle&#39;, &#39;Plate&#39;, &#39;Plastic Bottle&#39;,</span></span>
<span class="line"><span>                    &#39;Storage Battery&#39;, &#39;Toothbrush&#39;, &#39;Umbrella&#39;,&#39;tap&#39;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>WASTE_CLASS_NAMES = [&#39;residual_waste&#39;, &#39;food_waste&#39;, &#39;hazardous_waste&#39;, &#39;recyclable_waste&#39;,&#39;tap&#39;]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>WASTE_CLASSES = {</span></span>
<span class="line"><span>    &#39;food_waste&#39;: (&#39;Banana Peel&#39;, &#39;Broken Bones&#39;, &#39;Ketchup&#39;),</span></span>
<span class="line"><span>    &#39;hazardous_waste&#39;: (&#39;Marker&#39;, &#39;Oral Liquid Bottle&#39;, &#39;Storage Battery&#39;),</span></span>
<span class="line"><span>    &#39;recyclable_waste&#39;: (&#39;Plastic Bottle&#39;, &#39;Toothbrush&#39;, &#39;Umbrella&#39;),</span></span>
<span class="line"><span>    &#39;residual_waste&#39;: (&#39;Plate&#39;, &#39;Cigarette End&#39;, &#39;Disposable Chopsticks&#39;),</span></span>
<span class="line"><span>    &#39;tap&#39;:(&#39;tap&#39;)</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>COLORS = {</span></span>
<span class="line"><span>    &#39;recyclable_waste&#39;: (0, 0, 255),</span></span>
<span class="line"><span>    &#39;hazardous_waste&#39;: (255, 0, 0),</span></span>
<span class="line"><span>    &#39;food_waste&#39;: (0, 255, 0),</span></span>
<span class="line"><span>    &#39;residual_waste&#39;: (80, 80, 80),</span></span>
<span class="line"><span>    &#39;tap&#39; : (200,220,100)</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>As shown in the above figure, there are a total of 13 types of waste categories.</p>`,2),e("p",{"lineno-start":"44"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRT_NUM_CLASSES = 13</span></span></code></pre></div><p>Names of the waste model categories.</p>',2),e("p",{"lineno-start":"45"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TRT_CLASS_NAMES = ( &#39;Banana Peel&#39;,&#39;Broken Bones&#39;, &#39;Cigarette End&#39;, &#39;Disposable Chopsticks&#39;,</span></span>
<span class="line"><span>                   &#39;Ketchup&#39;, &#39;Marker&#39;, &#39;Oral Liquid Bottle&#39;, &#39;Plate&#39;, &#39;Plastic Bottle&#39;,</span></span>
<span class="line"><span>                    &#39;Storage Battery&#39;, &#39;Toothbrush&#39;, &#39;Umbrella&#39;,&#39;tap&#39;)</span></span></code></pre></div><p>Names of the waste categories.</p>`,2),e("p",{"lineno-start":"49"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WASTE_CLASS_NAMES = [&#39;residual_waste&#39;, &#39;food_waste&#39;, &#39;hazardous_waste&#39;, &#39;recyclable_waste&#39;,&#39;tap&#39;]</span></span></code></pre></div><p>Classification of waste models.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WASTE_CLASSES = {</span></span>
<span class="line"><span>    &#39;food_waste&#39;: (&#39;Banana Peel&#39;, &#39;Broken Bones&#39;, &#39;Ketchup&#39;),</span></span>
<span class="line"><span>    &#39;hazardous_waste&#39;: (&#39;Marker&#39;, &#39;Oral Liquid Bottle&#39;, &#39;Storage Battery&#39;),</span></span>
<span class="line"><span>    &#39;recyclable_waste&#39;: (&#39;Plastic Bottle&#39;, &#39;Toothbrush&#39;, &#39;Umbrella&#39;),</span></span>
<span class="line"><span>    &#39;residual_waste&#39;: (&#39;Plate&#39;, &#39;Cigarette End&#39;, &#39;Disposable Chopsticks&#39;),</span></span>
<span class="line"><span>    &#39;tap&#39;:(&#39;tap&#39;)</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Drawing colors corresponding to different waste categories.</p>`,4),e("p",{"lineno-start":"59"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>COLORS = {</span></span>
<span class="line"><span>    &#39;recyclable_waste&#39;: (0, 0, 255),</span></span>
<span class="line"><span>    &#39;hazardous_waste&#39;: (255, 0, 0),</span></span>
<span class="line"><span>    &#39;food_waste&#39;: (0, 255, 0),</span></span>
<span class="line"><span>    &#39;residual_waste&#39;: (80, 80, 80),</span></span>
<span class="line"><span>    &#39;tap&#39; : (200,220,100)</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>③ Initiate WasteClassificationNode Class (Waste Sorting Class)</p><p>The waste_classification node is launched in the main function of the executable program, implemented through the WasteClassificationNodes class. If the node remains active, it continuously calls the image_process function within this class. Throughout the program&#39;s operation, parameters are also initialized using the WasteClassificationNode class, which facilitates the implementation of the image_process function in the node.</p>`,3),e("p",{"lineno-start":"358"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if __name__ == &quot;__main__&quot;:</span></span>
<span class="line"><span>    node = WasteClassificationNode(&quot;waste_classification&quot;, log_level=rospy.INFO)</span></span>
<span class="line"><span>    while not rospy.is_shutdown():</span></span>
<span class="line"><span>        node.image_process()</span></span></code></pre></div><p>④ Initialization Function of WasteClassificationNode Node</p>`,2),e("p",{"lineno-start":"67"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>class WasteClassificationNode:</span></span>
<span class="line"><span>    def __init__(self, node_name, log_level=rospy.INFO):</span></span>
<span class="line"><span>        rospy.init_node(node_name, anonymous=True, log_level=log_level)</span></span>
<span class="line"><span>        self.lock = threading.RLock()</span></span>
<span class="line"><span>        self.K = None</span></span>
<span class="line"><span>        self.D = None</span></span>
<span class="line"><span>        self.waste_class_name = None</span></span>
<span class="line"><span>        config = rospy.get_param(CONFIG_NAME)</span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span>
<span class="line"><span>        self.extristric = None</span></span>
<span class="line"><span>        self.roi = None</span></span>
<span class="line"><span>        self.moving_step = 0</span></span>
<span class="line"><span>        self.status = 1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.pick_pitch = 80</span></span>
<span class="line"><span>        self.place_pitch = 0</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.target = None</span></span>
<span class="line"><span>        self.count = 0</span></span>
<span class="line"><span>        self.last_card = None</span></span>
<span class="line"><span>        self.endpoint = None</span></span>
<span class="line"><span>        self.image_queue = queue.Queue(maxsize=2)</span></span>
<span class="line"><span>        self.fps = fps.FPS()</span></span></code></pre></div><p>In the program screenshot above, it includes the gripper posture angle during the robotic arm&#39;s gripping state, referred to as pick_pitch, as well as the height for placing, called place_pitch.</p>`,2),e("ul",{"lineno-start":"91"},[e("li",null,[s("Creating the "),e("code",null,"servos_pub"),s(" Publisher: The "),e("code",null,"rospy.Publisher"),s(" is used to create a message publisher.")])],-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.servos_pub = rospy.Publisher(&quot;/controllers/multi_id_pos_dur&quot;, MultiRawIdPosDur, queue_size=1)</span></span></code></pre></div><p>The first parameter, <code>/controllers/multi_id_pos_dur</code>, indicates the topic name for servo control.</p><p>The second parameter, <code>MultiRawIdPosDur</code>, specifies the message type.</p><p>The third parameter, <code>queue_size=1</code>, sets the size of the message queue.</p>',4),e("ul",{"lineno-start":"92"},[e("li",null,"Defining `self.camera_info_sub` to Receive Camera Information: The `rospy.Subscriber` is used to create a message subscriber.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        camera_info_topic = rospy.get_param(&#39;~camera_info_topic&#39;, &#39;/camera/camera_info&#39;)</span></span>
<span class="line"><span>        self.camera_info_sub = rospy.Subscriber(camera_info_topic, CameraInfo, self.camera_info_callback, queue_size=1) #订阅相机内参(subscribe camera intrinsic parameter)</span></span></code></pre></div><p>The first parameter, <code>camera_info_topic</code>, indicates the topic name for receiving image data.</p><p>The second parameter, <code>CameraInfo</code>, specifies the message type.</p><p>The third parameter indicates the function <code>self.camera_info_callback</code> that will handle the returned images.</p><p>The fourth parameter, <code>queue_size=1</code>, sets the size of the message queue.</p><p>⑤ Calculating the World Coordinates of the Four Corners of the Recognition Area</p>`,6),e("p",{"lineno-start":"106"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        # 识别区域的四个角的世界坐标(the world coordinates of four corners in recognition region)</span></span>
<span class="line"><span>        white_area_cam = config[&#39;white_area_pose_cam&#39;]</span></span>
<span class="line"><span>        white_area_center = config[&#39;white_area_pose_world&#39;]</span></span>
<span class="line"><span>        self.white_area_center = white_area_center</span></span>
<span class="line"><span>        self.white_area_cam = white_area_cam</span></span>
<span class="line"><span>        white_area_height = config[&#39;white_area_world_size&#39;][&#39;height&#39;]</span></span>
<span class="line"><span>        white_area_width = config[&#39;white_area_world_size&#39;][&#39;width&#39;]</span></span>
<span class="line"><span>        white_area_lt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_lb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        self.get_endpoint()</span></span>
<span class="line"><span>        corners_cam =  np.matmul(np.linalg.inv(np.matmul(self.endpoint, config[&#39;hand2cam_tf_matrix&#39;])), [white_area_lt, white_area_lb, white_area_rb, white_area_rt, white_area_center])</span></span>
<span class="line"><span>        corners_cam = np.matmul(np.linalg.inv(white_area_cam), corners_cam)</span></span>
<span class="line"><span>        corners_cam = corners_cam[:, :3, 3:].reshape((-1, 3))</span></span>
<span class="line"><span>        tvec, rmat = config[&#39;extristric&#39;]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        while self.K is None or self.D is None: # 等待获取相机内参(wait for obtaining camera intrinsic parameters)</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span>
<span class="line"><span>        center_imgpts, jac = cv2.projectPoints(corners_cam[-1:], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.center_imgpts = np.int32(center_imgpts).reshape(2)</span></span>
<span class="line"><span>        tvec, rmat = extristric_plane_shift(np.array(tvec).reshape((3, 1)), np.array(rmat), 0.04)</span></span>
<span class="line"><span>        self.extristric = tvec, rmat</span></span>
<span class="line"><span>        imgpts, jac = cv2.projectPoints(corners_cam[:-1], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.imgpts = np.int32(imgpts).reshape(-1, 2)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"107"},[e("li",null,"Obtain the camera coordinates and world coordinates of the white area, and store them in the class attributes.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_cam = config[&#39;white_area_pose_cam&#39;]</span></span>
<span class="line"><span>        white_area_center = config[&#39;white_area_pose_world&#39;]</span></span>
<span class="line"><span>        self.white_area_center = white_area_center</span></span>
<span class="line"><span>        self.white_area_cam = white_area_cam</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"111"},[e("li",null,"Retrieve the height and width of the white area.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_height = config[&#39;white_area_world_size&#39;][&#39;height&#39;]</span></span>
<span class="line"><span>        white_area_width = config[&#39;white_area_world_size&#39;][&#39;width&#39;]</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"113"},[e("li",null,"Use the xyz_euler_to_mat function to transform the four corner points of the white area from the world coordinate system to the camera coordinate system.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_lt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_lb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"117"},[e("li",null,[s("Call the "),e("code",null,"self.get_endpoint()"),s(" function to obtain the current pose of the end effector. Then, perform matrix operations to transform the corner points of the white area from the camera coordinate system to the world coordinate system, storing the results in "),e("code",null,"corners_cam"),s(". Obtain the extrinsic parameters and assign them to "),e("code",null,"tvec"),s(" and "),e("code",null,"rmat"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.get_endpoint()</span></span>
<span class="line"><span>        corners_cam =  np.matmul(np.linalg.inv(np.matmul(self.endpoint, config[&#39;hand2cam_tf_matrix&#39;])), [white_area_lt, white_area_lb, white_area_rb, white_area_rt, white_area_center])</span></span>
<span class="line"><span>        corners_cam = np.matmul(np.linalg.inv(white_area_cam), corners_cam)</span></span>
<span class="line"><span>        corners_cam = corners_cam[:, :3, 3:].reshape((-1, 3))</span></span>
<span class="line"><span>        tvec, rmat = config[&#39;extristric&#39;]</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"123"},[e("li",null,"Obtain the hand-to-camera transformation matrix.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        while self.K is None or self.D is None: # 等待获取相机内参(wait for obtaining camera intrinsic parameters)</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"126"},[e("li",null,"Obtain the hand-to-camera transformation matrix.")],-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span></code></pre></div>',1),e("ul",{"lineno-start":"127"},[e("li",null,[s("Project the last corner point (corners_cam[-1:]) from the camera coordinate system onto the image plane to obtain the center point coordinates on the image, referred to as "),e("code",null,"center_imgpts"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        center_imgpts, jac = cv2.projectPoints(corners_cam[-1:], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.center_imgpts = np.int32(center_imgpts).reshape(2)</span></span></code></pre></div><p>The projection utilizes the camera intrinsic parameters <code>self.K</code> and <code>self.D</code>, along with the extrinsic parameters <code>rmat</code>and <code>tvec</code>. The projected center point image coordinates are stored in <code>self.center_imgpts</code></p>`,2),e("ul",{"lineno-start":"129"},[e("li",null,[s("Call the "),e("code",null,"extristric_plane_shift"),s(" function to fine-tune the translation vector and rotation matrix. The adjusted translation vector and rotation matrix are stored in "),e("code",null,"self.extristric"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        tvec, rmat = extristric_plane_shift(np.array(tvec).reshape((3, 1)), np.array(rmat), 0.04)</span></span>
<span class="line"><span>        self.extristric = tvec, rmat</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"131"},[e("li",null,[s("Project the other corner points, excluding the last one ("),e("code",null,"corners_cam[:-1]"),s("), from the camera coordinate system onto the image plane to obtain a set of image coordinates "),e("code",null,"imgpts"),s(". The projected image coordinates are stored in "),e("code",null,"self.imgpts"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        imgpts, jac = cv2.projectPoints(corners_cam[:-1], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.imgpts = np.int32(imgpts).reshape(-1, 2)</span></span></code></pre></div><p>⑥ Calculating the ROI Area</p><p>Obtain the maximum and minimum values of the x and y axes from the image. Use np.maximum to compare these values with 0, resulting in an array roi that contains only non-negative values.</p>`,3),e("p",{"lineno-start":"135"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        x_min = min(self.imgpts, key=lambda p: p[0])[0]  # x轴最小值(the minimum value of X-axis)</span></span>
<span class="line"><span>        x_max = max(self.imgpts, key=lambda p: p[0])[0]  # x轴最大值(the maximum value of X-axis)</span></span>
<span class="line"><span>        y_min = min(self.imgpts, key=lambda p: p[1])[1]  # y轴最小值(the minimum value of Y-axis)</span></span>
<span class="line"><span>        y_max = max(self.imgpts, key=lambda p: p[1])[1]  # y轴最大值(the maximum value of Y-axis)</span></span>
<span class="line"><span>        roi = np.maximum(np.array([y_min, y_max, x_min, x_max]), 0)</span></span>
<span class="line"><span>        self.roi = roi</span></span></code></pre></div><p>⑦ Creating the Action Client and Image Information Subscriber</p><p>A simple action client named action_client has been created to publish movement actions to the /grasp topic.</p>`,3),e("p",{"lineno-start":"143"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.action_client = actionlib.SimpleActionClient(&#39;/grasp&#39;, MoveAction)</span></span>
<span class="line"><span>        source_image_topic = rospy.get_param(&#39;~source_image_topic&#39;, &#39;/camera/image_raw&#39;)</span></span>
<span class="line"><span>        self.image_sub = rospy.Subscriber(source_image_topic, RosImage, self.image_callback, queue_size=1)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;启动完成\\r\\n\\r\\n&quot;)</span></span></code></pre></div><p>Retrieve the image topic name from the ROS parameter server, which defaults to /camera/image_raw, and create a subscriber to subscribe to this image topic. Use rospy.loginfo to print a message indicating that the node has started successfully.</p><p>⑧ camera_info_callback Function</p><p>The function extracts the camera&#39;s intrinsic matrix from the received camera information message and stores it in the class member variable \`self.K\`.</p>`,4),e("p",{"lineno-start":"149"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def camera_info_callback(self, msg): # 相机内参回调(callback camera intrinsic parameters)</span></span>
<span class="line"><span>        with self.lock:</span></span>
<span class="line"><span>            K = np.matrix(msg.K).reshape(1, -1, 3)</span></span>
<span class="line"><span>            D = np.array(msg.D)</span></span>
<span class="line"><span>            new_K, roi = cv2.getOptimalNewCameraMatrix(K, D, (640, 480), 0, (640, 480))</span></span>
<span class="line"><span>            self.K, self.D = np.matrix(new_K), np.zeros((5, 1))</span></span></code></pre></div><p><code>msg.K</code>: Represents the intrinsic matrix of the camera within the camera information message.</p><p><code>np.matrix(msg.K</code>: Converts the camera intrinsic matrix to a NumPy matrix.</p><p><code>.reshape(1, -1, 3)</code>: Reshapes the matrix to a 1xN format, where N is the number of elements in the matrix, with each element containing three values (e.g., rows, columns, focal length, and other parameters).</p><p><code>self.D = np.array(msg.D)</code>: Converts the distortion coefficients <code>D</code> from the received camera intrinsics into a NumPy array.</p><p>⑨ image_process Function</p><p>The main function of the image_process method is to perform processing and recognition on images.</p>`,7),e("p",{"lineno-start":"265"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def image_process(self):</span></span>
<span class="line"><span>        ros_image = self.image_queue.get(block=True)</span></span>
<span class="line"><span>        # 将ros格式图像转换为opencv格式(convert the image from ros format to opencv format)</span></span>
<span class="line"><span>        rgb_image = np.ndarray(shape=(ros_image.height, ros_image.width, 3), dtype=np.uint8, buffer=ros_image.data)</span></span>
<span class="line"><span>        result_image = np.copy(rgb_image)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        if self.center_imgpts is not None:</span></span>
<span class="line"><span>            cv2.line(result_image, (self.center_imgpts[0] - 10, self.center_imgpts[1]), (self.center_imgpts[0] + 10, self.center_imgpts[1]), (255, 255, 0), 2)</span></span>
<span class="line"><span>            cv2.line(result_image, (self.center_imgpts[0], self.center_imgpts[1] - 10), (self.center_imgpts[0], self.center_imgpts[1] + 10), (255, 255, 0), 2)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        try:</span></span>
<span class="line"><span>            if self.moving_step == 0 and self.roi is not None and self.K is not None and self.D is not None:</span></span>
<span class="line"><span>                roi_area_mask = np.zeros(shape=(ros_image.height, ros_image.width, 1), dtype=np.uint8)</span></span>
<span class="line"><span>                roi_area_mask = cv2.drawContours(roi_area_mask, [self.imgpts], -1, 255, cv2.FILLED)                </span></span>
<span class="line"><span>                rgb_image = cv2.bitwise_and(rgb_image, rgb_image, mask=roi_area_mask)  # 和原图做遮罩，保留需要识别的区域(create a mask based on the original image to retain the region to be recognized)</span></span>
<span class="line"><span>                #roi_img = rgb_image[self.roi[0]:self.roi[1], self.roi[2]:self.roi[3]]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                boxes, confs, classes = self.yolov5.inference(cv2.cvtColor(rgb_image, cv2.COLOR_RGB2BGR))</span></span>
<span class="line"><span>                cards = []</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                for box, cls_conf, cls_id in zip(boxes, confs, classes):</span></span>
<span class="line"><span>                    x1 = box[0] #+ self.roi[2]</span></span>
<span class="line"><span>                    y1 = box[1] #+ self.roi[0]</span></span>
<span class="line"><span>                    x2 = box[2] #+ self.roi[2]</span></span>
<span class="line"><span>                    y2 = box[3] #+ self.roi[0]</span></span>
<span class="line"><span>                    waste_name = TRT_CLASS_NAMES[cls_id]</span></span>
<span class="line"><span>                    if waste_name == &quot;tap&quot;:</span></span>
<span class="line"><span>                        continue</span></span>
<span class="line"><span>                    waste_class_name = &#39;&#39;</span></span>
<span class="line"><span>                    for k, v in WASTE_CLASSES.items():</span></span>
<span class="line"><span>                        if waste_name in v:</span></span>
<span class="line"><span>                            waste_class_name = k</span></span>
<span class="line"><span>                            break</span></span>
<span class="line"><span>                    cards.append((cls_conf, [x1, y1, x2, y2], waste_class_name))</span></span>
<span class="line"><span>                    result_image = cv2.putText(result_image, waste_name + &quot; &quot; + str(float(cls_conf))[:4], (int(x1), int(y1) - 5),</span></span>
<span class="line"><span>                                               cv2.FONT_HERSHEY_SIMPLEX, 0.7, COLORS[waste_class_name], 2)</span></span>
<span class="line"><span>                    result_image = cv2.rectangle(result_image, (int(x1), int(y1)), (int(x2), int(y2)), COLORS[waste_class_name], 3)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"268"},[e("li",null,"Obtain the ROS-formatted image data and convert it to OpenCV format, creating a copy of the image.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        rgb_image = np.ndarray(shape=(ros_image.height, ros_image.width, 3), dtype=np.uint8, buffer=ros_image.data)</span></span>
<span class="line"><span>        result_image = np.copy(rgb_image)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"271"},[e("li",null,[s("If "),e("code",null,"self.center_imgpts"),s(" is not "),e("code",null,"None"),s(", it indicates that there is center point image coordinate information available. Two crosshairs are drawn on "),e("code",null,"result_image"),s(" using "),e("code",null,"cv2.line"),s(" to mark the center point.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        if self.center_imgpts is not None:</span></span>
<span class="line"><span>            cv2.line(result_image, (self.center_imgpts[0] - 10, self.center_imgpts[1]), (self.center_imgpts[0] + 10, self.center_imgpts[1]), (255, 255, 0), 2)</span></span>
<span class="line"><span>            cv2.line(result_image, (self.center_imgpts[0], self.center_imgpts[1] - 10), (self.center_imgpts[0], self.center_imgpts[1] + 10), (255, 255, 0), 2)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"275"},[e("li",null,"Begin image processing if the following conditions are met.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        try:</span></span>
<span class="line"><span>            if self.moving_step == 0 and self.roi is not None and self.K is not None and self.D is not None:</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"277"},[e("li",null,"Create a blank mask image of the same size as the original image to store the mask for the region of interest.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                roi_area_mask = np.zeros(shape=(ros_image.height, ros_image.width, 1), dtype=np.uint8)</span></span>
<span class="line"><span>                roi_area_mask = cv2.drawContours(roi_area_mask, [self.imgpts], -1, 255, cv2.FILLED)                </span></span>
<span class="line"><span>                rgb_image = cv2.bitwise_and(rgb_image, rgb_image, mask=roi_area_mask)  # 和原图做遮罩，保留需要识别的区域(create a mask based on the original image to retain the region to be recognized)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"282"},[e("li",null,[e("p",null,"Draw the contours of the region of interest on the mask image to create a filled white area.")]),e("li",null,[e("p",null,"Perform a bitwise AND operation between the original color image and the mask image to obtain an image that retains only the content of the region of interest.")]),e("li",null,[e("p",null,"Extract the region of interest from the resulting image to form a new image, roi_img.")]),e("li",null,[e("p",null,[s("Use the YOLOv5 model for object detection on "),e("code",null,"roi_img"),s(". Detected objects will be stored in "),e("code",null,"boxes"),s(", "),e("code",null,"confs"),s(", and "),e("code",null,"classes"),s(", which will be used for subsequent processing and drawing.")])])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                boxes, confs, classes = self.yolov5.inference(cv2.cvtColor(rgb_image, cv2.COLOR_RGB2BGR))</span></span>
<span class="line"><span>                cards = []</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"285"},[e("li",null,"Iterate through the detected objects to perform the following actions:")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                for box, cls_conf, cls_id in zip(boxes, confs, classes):</span></span>
<span class="line"><span>                    x1 = box[0] #+ self.roi[2]</span></span>
<span class="line"><span>                    y1 = box[1] #+ self.roi[0]</span></span>
<span class="line"><span>                    x2 = box[2] #+ self.roi[2]</span></span>
<span class="line"><span>                    y2 = box[3] #+ self.roi[0]</span></span>
<span class="line"><span>                    waste_name = TRT_CLASS_NAMES[cls_id]</span></span>
<span class="line"><span>                    if waste_name == &quot;tap&quot;:</span></span>
<span class="line"><span>                        continue</span></span>
<span class="line"><span>                    waste_class_name = &#39;&#39;</span></span>
<span class="line"><span>                    for k, v in WASTE_CLASSES.items():</span></span>
<span class="line"><span>                        if waste_name in v:</span></span>
<span class="line"><span>                            waste_class_name = k</span></span>
<span class="line"><span>                            break</span></span>
<span class="line"><span>                    cards.append((cls_conf, [x1, y1, x2, y2], waste_class_name))</span></span>
<span class="line"><span>                    result_image = cv2.putText(result_image, waste_name + &quot; &quot; + str(float(cls_conf))[:4], (int(x1), int(y1) - 5),</span></span>
<span class="line"><span>                                               cv2.FONT_HERSHEY_SIMPLEX, 0.7, COLORS[waste_class_name], 2)</span></span>
<span class="line"><span>                    result_image = cv2.rectangle(result_image, (int(x1), int(y1)), (int(x2), int(y2)), COLORS[waste_class_name], 3)</span></span></code></pre></div><p>Calculate the coordinates of the object in the original image, using <code>x1</code>, <code>y1</code>, <code>x2</code>, and <code>y2</code> to represent the coordinates of the top-left and bottom-right corners of the object.</p><p>Retrieve the object class name <code>waste_name</code> using <code>cls_id</code>, and look up the corresponding class name <code>waste_class_name</code> in the predefined class dictionary <code>WASTE_CLASSES</code>.</p><p>Store the object&#39;s relevant information (confidence, coordinates, class name) as a tuple in the <code>cards</code> list.</p><p>Use <code>cv2.putText</code> to draw the object&#39;s class name and confidence on the image.</p><p>Use <code>cv2.rectangle</code> to draw a rectangle around the detected object in the image.</p><p>These steps will draw and label the detected objects, displaying the identified waste categories and their locations on the image. The processing steps will be conducted within the region of interest to limit the scope of object recognition.</p>`,7),e("ul",{"lineno-start":"303"},[e("li",null,[s("If the number of detected objects is greater than 0, sort the "),e("code",null,"cards"),s(" list by confidence in descending order.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                if len(cards) &gt; 0:</span></span>
<span class="line"><span>                    cards = sorted(cards, key=lambda c: c[0], reverse=True)</span></span>
<span class="line"><span>                    if self.last_card is None:</span></span>
<span class="line"><span>                        self.last_card = cards[0]</span></span>
<span class="line"><span>                    center_last = box_center(self.last_card[1])</span></span>
<span class="line"><span>                    cards = sorted(cards, key=lambda c: distance(box_center(c[1]), center_last))</span></span>
<span class="line"><span>                    center = box_center(cards[0][1])</span></span></code></pre></div><p>If no target was previously recognized (<code>last_card</code> is <code>None</code>), the first target in the list will be set as <code>last_card</code>.</p><p>Obtain the center position <code>center_last</code> of the previously recognized target by calling the function <code>box_center(self.last_card[1])</code>, where <code>self.last_card[1]</code> represents the coordinates of the target box.</p><p>Sort the <code>cards</code> list by the distance between the target centers and <code>center_last</code> using the <code>sorted</code> function with a <code>key</code> parameter that specifies the sorting criterion. A lambda function is used to compute the distance, ordering from nearest to farthest.</p><p>Get the center position <code>center</code> of the target that ranks first in the list by calling <code>box_center(cards[0][1])</code>, where <code>cards[0][1]</code> represents the coordinates of the first target box in the list.</p>`,5),e("ul",{"lineno-start":"310"},[e("li",null,"If the distance between the current target center and the last recognized target center is less than 50 pixels, and this condition is met for more than 40 consecutive counts, indicating good stability for the current target, execute the following steps:")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                    if distance(center, center_last) &lt; 50:</span></span>
<span class="line"><span>                        self.count += 1</span></span>
<span class="line"><span>                        if self.count &gt; 40:</span></span>
<span class="line"><span>                            projection_matrix = np.row_stack((np.column_stack((self.extristric[1], self.extristric[0])), np.array([[0, 0, 0, 1]])))</span></span>
<span class="line"><span>                            world_pose = pixels_to_world([center, ], self.K, projection_matrix)[0]  # 像素坐标相对于识别区域中心的相对坐标(the relative coordinate of the pixel coordinate with respect to the center of the recognition region)</span></span>
<span class="line"><span>                            world_pose[1] = -world_pose[1]</span></span>
<span class="line"><span>                            world_pose[2] = 0.04</span></span>
<span class="line"><span>                            world_pose = np.matmul(self.white_area_center, xyz_euler_to_mat(world_pose, (0, 0, 0)))  # 转换到相机相对坐标(convert to the relative coordinate of the camera)</span></span>
<span class="line"><span>                            world_pose[2] = 0.04</span></span>
<span class="line"><span>                            #world_pose = np.matmul(self.white_area_cam, xyz_euler_to_mat(world_pose, (0, 0, 0)))  # 转换到相机相对坐标(convert to the relative coordinate of the camera)</span></span>
<span class="line"><span>                            # pose_end = np.matmul(self.hand2cam_tf_matrix, world_pose)  # 转换的末端相对坐标(convert to the relative coordinate of the end effector)</span></span>
<span class="line"><span>                            # pose_world = np.matmul(self.endpoint, pose_end)  # 转换到机械臂世界坐标(convert to the world coordinate of the robotic arm)</span></span>
<span class="line"><span>                            pose_t, pose_R = mat_to_xyz_euler(world_pose)</span></span>
<span class="line"><span>                            #pose_t[0] = pose_t[0] - 0.01  # 卡片上的图案不在画面中心(the design on the card is not in the center of the frame)</span></span>
<span class="line"><span>                            params = rospy.get_param(os.path.join(POSITIONS_PATH, &#39;waste_classification&#39;))</span></span>
<span class="line"><span>                            </span></span>
<span class="line"><span>                            for i in range(3):</span></span>
<span class="line"><span>                                pose_t[i] = pose_t[i] + params[&#39;offset&#39;][i]</span></span>
<span class="line"><span>                                pose_t[i] = pose_t[i] * params[&#39;scale&#39;][i]</span></span>
<span class="line"><span>                                print(pose_t)</span></span>
<span class="line"><span>                            self.target = cards[0]</span></span>
<span class="line"><span>                            self.moving_step = 1</span></span>
<span class="line"><span>                            self.status = 1</span></span>
<span class="line"><span>                            threading.Thread(target=self.action_starting, args=(pose_t, pose_R)).start()</span></span></code></pre></div><p>Construct the projection matrix <code>projection_matrix</code>.</p><p>Convert the pixel coordinates to relative coordinates concerning the center of the recognized area to obtain <code>world_pose</code>.</p><p>Transform <code>world_pose</code> to the camera-relative coordinate system.</p><p>Update the Z-coordinate in <code>world_pose</code> to a fixed value of 0.04.</p><p>Convert the transformed coordinates in \`world_pose\` to displacement and rotation angles <code>pose_t</code> and <code>pose_R</code>.</p><p>Set the recognized target as <code>target</code>.</p><p>Set <code>moving_step</code> to 1 to indicate the start of the grasping action.</p><p>Use a thread to execute the grasping action in the background.</p>`,9),e("ul",{"lineno-start":"335"},[e("li",null,[e("p",null,[s("If the distance between the current target center and the last recognized target center exceeds 50 pixels or if no targets are detected, reset the consecutive count to 0 and set "),e("code",null,"last_card"),s(" to "),e("code",null,"None"),s(".")])]),e("li",null,[e("p",null,"If an exception occurs during processing, log the exception information.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                    else:</span></span>
<span class="line"><span>                        self.count = 0</span></span>
<span class="line"><span>                    self.last_card = cards[0]</span></span>
<span class="line"><span>                else:</span></span>
<span class="line"><span>                    self.count = 0</span></span>
<span class="line"><span>                    self.last_card = None</span></span>
<span class="line"><span>        except Exception as e:</span></span>
<span class="line"><span>            rospy.logerr(str(e))</span></span></code></pre></div><p><code>self.fps.update()</code>: Update the frame rate statistics.</p><p><code>result_image = self.fps.show_fps(result_image)</code>: Pass the current frame&#39;s image to the frame rate statistics object to display frame rate information on the image.</p><p><code>cv2.imshow</code>: Use the OpenCV library to display the processed image, with the first parameter being the window name and the second parameter being the image to display. Here, the window name is <code>waste_classification</code>, and <code>cv2.cvtColor(result_image, cv2.COLOR_RGB2BGR)</code> converts the RGB-format image to BGR format since OpenCV defaults to BGR.</p><p><code>cv2.waitKey(1)</code>: Wait for 1 millisecond and then check for keyboard input.</p>`,5),e("p",{"lineno-start":"345"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.fps.update()</span></span>
<span class="line"><span>        result_image = self.fps.show_fps(result_image)</span></span>
<span class="line"><span>        cv2.imshow(&quot;waste_classification&quot;, cv2.cvtColor(result_image, cv2.COLOR_RGB2BGR))</span></span>
<span class="line"><span>        cv2.waitKey(1)</span></span></code></pre></div><p>⑩ get_endpoint Function</p>`,2),e("ul",{"lineno-start":"157"},[e("li",null,"This function retrieves the pose information of the robot's end effector.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def get_endpoint(self):</span></span>
<span class="line"><span>        endpoint = rospy.ServiceProxy(&#39;/kinematics/get_current_pose&#39;, GetRobotPose)().pose</span></span>
<span class="line"><span>        self.endpoint = xyz_quat_to_mat([endpoint.position.x, endpoint.position.y, endpoint.position.z],</span></span>
<span class="line"><span>                                        [endpoint.orientation.w, endpoint.orientation.x, endpoint.orientation.y, endpoint.orientation.z])</span></span></code></pre></div><ul><li><p>endpoint: It uses a ROS service proxy to call the <code>/kinematics/get_current_pose</code> service, which obtains the current pose information of the robot and stores it in the <code>endpoint</code> variable.</p></li><li><p>The function calls <code>xyz_quat_to_mat</code> to convert the retrieved pose information into a pose matrix, which is then stored in the <code>self.endpoint</code> attribute.</p></li></ul><p>⑪ done_callback Function</p><p>This function acts as a callback when an action is completed. Based on the action&#39;s status and result, it executes a series of operations to handle different situations.</p>`,4),e("p",{"lineno-start":"162"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def done_callback(self, state, result):  # 动作执行完毕回调(callback for action execution completion)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;state:%f&quot;, state)</span></span>
<span class="line"><span>        if not result.result.complete:  # 如果在移动中被取消，需要回到初始位置(If the action is cancelled during movement, return to the initial position)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((1, 500), ))</span></span>
<span class="line"><span>            rospy.sleep(1)</span></span>
<span class="line"><span>            actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((10, 200), ))</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span>
<span class="line"><span>        elif self.moving_step != 1:</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((10, 200), ))</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        if self.finish_percent == 1:  # 如果完整的完成移动(if the movement is completed in full)</span></span>
<span class="line"><span>            if self.moving_step == 1:  # 如果完成了夹取(if the gripper has completed the grasping)</span></span>
<span class="line"><span>                actions.go_back(self.servos_pub)</span></span>
<span class="line"><span>                self.moving_step = 2</span></span>
<span class="line"><span>                goal = MoveGoal()</span></span>
<span class="line"><span>                goal.grasp.mode = &#39;place&#39;</span></span>
<span class="line"><span>                rospy.sleep(1)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[self.target[-1]] # 读取放置坐标(read placing coordinate)</span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 设置滑轨使能(enable the slider track)</span></span>
<span class="line"><span>                stepper.goto(stepper_position) # 驱动滑轨移动到放置位置(drive the slider track to move to the placing position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000 # 计算需要的时间(calculate the required time)</span></span>
<span class="line"><span>                rospy.sleep(stepper_time-0.5)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                target_position = TARGET_POSITION_STEPPER[self.target[-1]]</span></span>
<span class="line"><span>                goal.grasp.position.x = target_position[0]</span></span>
<span class="line"><span>                goal.grasp.position.y = target_position[1]</span></span>
<span class="line"><span>                goal.grasp.position.z = target_position[2]</span></span>
<span class="line"><span>                goal.grasp.pitch = self.place_pitch</span></span>
<span class="line"><span>                #goal.grasp.align_angle = 90  # yaw #- 20/1000* 240</span></span>
<span class="line"><span>                goal.grasp.grasp_approach.z = 0.06  # 放置时靠近的方向和距离(the direction and distance for approaching during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_retreat.z = 0.06  # 放置后后撤的方向和距离(the direction and distance of retreat during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_posture = 370  # 夹取前后夹持器的开合角度(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>                goal.grasp.pre_grasp_posture = 580</span></span>
<span class="line"><span>                self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)</span></span></code></pre></div><ul><li>If the movement is canceled during execution, the robotic arm will call <code>set_servos</code> in <code>bus_servo_control</code> to return the gimbal to its initial position. It will then call <code>go_home</code> in the actions module to return the robotic arm to its starting position. The <code>stepper</code> is used to control the motor of the sliding rail, moving the robotic arm mounted on the platform to the designated location. If the action is canceled during grasping, the robotic arm will call <code>set_servos</code> in <code>bus_servo_control</code> to release the gripper.</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if not result.result.complete:</span></span>
<span class="line"><span>    bus_servo_control.set_servos(self.servos_pub，500，((1，500)，))</span></span>
<span class="line"><span>    rospy.sleep(1)</span></span>
<span class="line"><span>    actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>    bus_servo_control.set_servos(self.servos_pub，500，((10，200)，))</span></span>
<span class="line"><span>    rospy.sleep(0.5)</span></span></code></pre></div><img loading="lazy" height="145" width="948" class="common_img" src="`+z+'"><ul><li>If the movement and grasping are completed successfully, the function sets the grasping mode to &#39;place&#39;, retrieves target position information from <code>target_position</code>, sets the alignment angle, and defines the approach and retreat distances during grasping, as well as the opening and closing angles of the gripper. Finally, it sends this target to the action client using <code>self.action_client.send_goal</code>.</li></ul><p><code>self.action_client</code> is an Action Client object.</p><p><code>send_goal()</code> is the method used to send the target to the server.</p><p><code>goal</code> represents the target to be sent.</p><p><code>self.done_callback</code> is the callback function invoked when the target is completed.</p><p><code>self.active_callback</code> is called when the server accepts the target and begins execution.</p><p><code>self.feedback_callback</code> is invoked when the server provides feedback information.</p>',11),e("p",{"lineno-start":"173"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        if self.finish_percent == 1:  # 如果完整的完成移动(if the movement is completed in full)</span></span>
<span class="line"><span>            if self.moving_step == 1:  # 如果完成了夹取(if the gripper has completed the grasping)</span></span>
<span class="line"><span>                actions.go_back(self.servos_pub)</span></span>
<span class="line"><span>                self.moving_step = 2</span></span>
<span class="line"><span>                goal = MoveGoal()</span></span>
<span class="line"><span>                goal.grasp.mode = &#39;place&#39;</span></span>
<span class="line"><span>                rospy.sleep(1)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[self.target[-1]] # 读取放置坐标(read placing coordinate)</span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 设置滑轨使能(enable the slider track)</span></span>
<span class="line"><span>                stepper.goto(stepper_position) # 驱动滑轨移动到放置位置(drive the slider track to move to the placing position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000 # 计算需要的时间(calculate the required time)</span></span>
<span class="line"><span>                rospy.sleep(stepper_time-0.5)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                target_position = TARGET_POSITION_STEPPER[self.target[-1]]</span></span>
<span class="line"><span>                goal.grasp.position.x = target_position[0]</span></span>
<span class="line"><span>                goal.grasp.position.y = target_position[1]</span></span>
<span class="line"><span>                goal.grasp.position.z = target_position[2]</span></span>
<span class="line"><span>                goal.grasp.pitch = self.place_pitch</span></span>
<span class="line"><span>                #goal.grasp.align_angle = 90  # yaw #- 20/1000* 240</span></span>
<span class="line"><span>                goal.grasp.grasp_approach.z = 0.06  # 放置时靠近的方向和距离(the direction and distance for approaching during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_retreat.z = 0.06  # 放置后后撤的方向和距离(the direction and distance of retreat during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_posture = 370  # 夹取前后夹持器的开合角度(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>                goal.grasp.pre_grasp_posture = 580</span></span>
<span class="line"><span>                self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"200"},[e("li",null,[s("If the robotic arm successfully grasps the garbage block, it calls "),e("code",null,"go_home"),s(" in the actions module to return to its initial position, resets the relevant variables, and uses the "),e("code",null,"stepper"),s(" to control the sliding rail motor, moving the robotic arm mounted on the platform to the designated position.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>            elif self.moving_step == 2:  # 如果完成了放置(if the placement is completed)</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>                actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>                rospy.sleep(1.5)</span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[self.target[-1]] # 读取放置坐标(read placing coordinate)</span></span>
<span class="line"><span>                print(self.target[-1])</span></span>
<span class="line"><span>                stepper.goto(-stepper_position) # 滑轨回到初始位置(the slider track returns to the initial position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000</span></span>
<span class="line"><span>                rospy.sleep(stepper_time)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 解除滑轨锁定(release slider track lock)</span></span>
<span class="line"><span>                print(&quot;FINISHED&quot;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>                self.get_endpoint()</span></span>
<span class="line"><span>                self.last_position = None</span></span>
<span class="line"><span>                self.target = None</span></span>
<span class="line"><span>                self.count = 0</span></span>
<span class="line"><span>                self.moving_step = 0</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"219"},[e("li",null,"If the action is canceled or the specified position cannot be reached, the go_home function in the actions module will be called to return the robotic arm to its initial position and reset the relevant variables.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        else:  # 如果被取消或者无法到达指定位置(if the action is cancelled, or it is unable to reach the specified position)</span></span>
<span class="line"><span>            actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>            self.moving_step = 0</span></span>
<span class="line"><span>            self.last_position = None</span></span></code></pre></div><p>⑫ active_callback Function</p><p>At the start of the movement, set a variable named start_move to True and log a message indicating that the movement has begun.</p>`,3),e("p",{"lineno-start":"224"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def active_callback(self):  # 运动开始回调(callback for starting the movement)</span></span>
<span class="line"><span>        self.start_move = True</span></span>
<span class="line"><span>        rospy.loginfo(&quot;start move&quot;)</span></span></code></pre></div><p>⑬ feedback_callback</p><p>The primary function of this callback function is to retrieve the execution progress information of the action and log this information.</p>`,3),e("p",{"lineno-start":"228"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def feedback_callback(self, msg):  # 动作执行进度回调(callback action execution progress)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;finish action: {:.2%}&quot;.format(msg.percent))</span></span>
<span class="line"><span>        self.finish_percent = msg.percent</span></span></code></pre></div><p>⑭ start_moving Function</p><p>Set the relevant parameters for the robotic arm to perform handling and stacking tasks. Create a <code>MoveGoal</code> object and assign these parameters to its properties. These parameters include the target position for grasping, orientation angles, grasping method, and more. Send this goal to the robotic arm control node through the action client to initiate the corresponding handling and stacking actions.</p>`,3),e("p",{"lineno-start":"232"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def start_moving(self, pose_t, pose_R):</span></span>
<span class="line"><span>        rospy.loginfo(&quot;开始搬运堆叠...&quot;)</span></span>
<span class="line"><span>        print(pose_t, pose_R)</span></span>
<span class="line"><span>        self.moving_step = 1</span></span>
<span class="line"><span>        goal = MoveGoal()</span></span>
<span class="line"><span>        goal.grasp.mode = &#39;pick&#39;</span></span>
<span class="line"><span>        # 物体坐标(object coordinate)</span></span>
<span class="line"><span>        goal.grasp.position.x = pose_t[0]</span></span>
<span class="line"><span>        goal.grasp.position.y = pose_t[1]</span></span>
<span class="line"><span>        goal.grasp.position.z = -0.032</span></span>
<span class="line"><span>        # 夹取时的姿态角(the posture angle during grasping)</span></span>
<span class="line"><span>        goal.grasp.pitch = self.pick_pitch</span></span>
<span class="line"><span>        goal.grasp.align_angle = 0  # 总是朝前(always moving forward)</span></span>
<span class="line"><span>        # 夹取时靠近的方向和距离(the direction and distance for approaching during grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_approach.z = 0.03</span></span>
<span class="line"><span>        # 夹取后后撤方向和距离(the direction and distance of retreat during grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_retreat.z = 0.05</span></span>
<span class="line"><span>        # 夹取前后夹持器的开合(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_posture = 580</span></span>
<span class="line"><span>        goal.grasp.pre_grasp_posture = 220</span></span>
<span class="line"><span>        self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)  # 发送夹取请求(send grasping request)</span></span>
<span class="line"><span>        rospy.sleep(2)</span></span></code></pre></div><p>⑮ image_callback Function</p><p>This is a callback function designed to handle the received ROS image messages. It takes a parameter of type <code>RosImage</code>, named <code>ros_image</code>, which represents the received image message. The <code>rospy.logdebug</code> function outputs a debug-level log message indicating that the image has been successfully received.</p><p>The line <code>except Exception as e:</code> is part of the exception handling mechanism. If an error occurs in the preceding code block, the code in this section will be executed.</p>`,4),e("p",{"lineno-start":"350"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def image_callback(self, ros_image: RosImage):</span></span>
<span class="line"><span>        rospy.logdebug(&#39;Received an image! &#39;)</span></span>
<span class="line"><span>        try:</span></span>
<span class="line"><span>            self.image_queue.put_nowait(ros_image)</span></span>
<span class="line"><span>        except Exception as e:</span></span>
<span class="line"><span>            e = e</span></span></code></pre></div><p id="anchor_10_4_5"></p><h3 id="_10-4-5-function-extension" tabindex="-1">10.4.5 Function Extension <a class="header-anchor" href="#_10-4-5-function-extension" aria-label="Permalink to &quot;10.4.5 Function Extension&quot;">​</a></h3><p>When we need to adjust the movement position of the slide rail and the placement position of the garbage block on the robotic arm, we can refer to the following steps to modify the parameters.</p><p>(1) Click the <strong>&quot;File&quot;</strong> icon on the system taskbar (on the left) to enter the file explorer.</p><img loading="lazy" height="288" width="61" class="common_img" src="`+l+'"><p>(2) Navigate to the package folder and double-click to open the <code>waste_classification_stepper.py</code> file.</p><img loading="lazy" height="218" width="771" class="common_img" src="'+j+'"><p>(3) As shown in the image below, the content highlighted in the green box represents the parameters for adjusting the movement position of the slide rail, while the content highlighted in the red box represents the placement position parameters for the garbage block.</p><img loading="lazy" height="386" width="542" class="common_img" src="'+D+'"><p>(4) You can then refer to the following two points to modify and adjust the placement position of the garbage block and the movement position of the slide rail platform.</p><ul><li><strong>Updating Slide Platform Position</strong></li></ul><p>To adjust the movement position of the slide platform, modify the program as follows:</p><p>(1) As shown in the image below, the dictionary TARGET_POSITION_STEPPER includes four keys: residual_waste, food_waste, hazardous_waste, and recyclable_waste. Each key has a corresponding value that includes four parameters.</p><img loading="lazy" height="123" width="405" class="common_img" src="'+V+'"><p>(2) Taking residual_waste as an example, [0.23, 0, 0.11] represents the target position in the robot&#39;s three-axis coordinates, with the values indicating the X, Y, and Z relative positions in meters. You can refer to the image for the specific positions along each axis.</p><img loading="lazy" height="673" width="956" class="common_img" src="'+n+'"><p>(3) If the placement is close to the center of the X-axis, you may slightly increase the X-axis value for residual_waste. For example, changing 0.23 to 0.25 moves the placement 2 cm further along the X-axis. Be cautious with large changes to ensure the robotic arm has enough safe movement space. Adjust the Y and Z-axis values similarly to control horizontal and vertical placement, respectively, with all values in meters.</p><p>(4) For food_waste, hazardous_waste, and recyclable_waste, you can follow similar steps as outlined for residual_waste.</p><ul><li><strong>Adjusting Slide Platform Position</strong></li></ul><p>To adjust the movement position of the slide platform, you can modify the program as shown in the following steps:</p><img loading="lazy" height="107" width="314" class="common_img" src="'+O+'"><p>(1) In the defined <strong>TARGET_POSITION</strong> dictionary, there are four keys: recyclable_waste, hazardous_waste, food_waste, and residual_waste. Each key has a single parameter value representing the slide platform&#39;s movement distance. These values correspond to different positions for each waste category, as shown in the program screenshot.</p><img loading="lazy" height="322" width="914" class="common_img" src="'+r+'"><p>(2) In the image, the robot&#39;s initial position on the slide platform is 0 (minimum) and the maximum is 5200. By adjusting the values associated with each waste category, you can change the slide platform&#39;s position.</p><p>(3) For example, if residual_waste is set to 2300 in the program, changing it to 2000 will move the platform to position 2000. Similarly, you can modify the position values for other waste categories by changing the respective values in the range [0–5200] to ensure the slide platform stays within a safe operating range.</p><h2 id="_10-5-tag-sorting" tabindex="-1">10.5 Tag Sorting <a class="header-anchor" href="#_10-5-tag-sorting" aria-label="Permalink to &quot;10.5 Tag Sorting&quot;">​</a></h2><h3 id="_10-5-1-program-logic" tabindex="-1">10.5.1 Program Logic <a class="header-anchor" href="#_10-5-1-program-logic" aria-label="Permalink to &quot;10.5.1 Program Logic&quot;">​</a></h3><p>This lesson demonstrates how to use a camera to recognize tags, then control a robotic arm to grab a tag block based on the recognized tag information and position, using inverse kinematics to calculate the required arm movements. The arm then drives along a track to place the tag block at a specified location.</p><p>AprilTag is a visual positioning marker similar to QR codes or barcodes.</p><p>First, we subscribe to the camera node&#39;s published data to obtain real-time image information. The image is then converted from RGB color space to grayscale, and the camera&#39;s internal parameters are read.</p><p>Next, we use the AprilTag library functions to detect AprilTags in the image, identifying the tag ID and retrieving the coordinates of the four corners and center of the tag. The position of the tag block is determined by calculating the coordinates from the recognized tag. Using inverse kinematics, we compute the angles of the servos needed to position the arm at the target location. The robotic arm is then controlled to grab the tag block and drive the track to place it at the corresponding position according to the tag&#39;s ID.</p><h3 id="_10-5-2-start-and-close-the-game" tabindex="-1">10.5.2 Start and Close the Game <a class="header-anchor" href="#_10-5-2-start-and-close-the-game" aria-label="Permalink to &quot;10.5.2 Start and Close the Game&quot;">​</a></h3><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p></p><p>The input commands are case-sensitive. Additionally, you can use the &quot;<strong>Tab</strong>&quot; key on the keyboard to auto-complete keywords.</p></div><p>(1) Refer to &quot;<a href="https://docs.hiwonder.com/projects/JetArm/en/latest/docs/1.Getting_Ready.html#development-environment-setup-and-configuration" target="_blank" rel="noreferrer"><strong>1. Getting Ready(JetArm User Manual)-&gt;1.6 Development Environment Setup and Configuration</strong></a>&quot; to establish a connection between the robotic arm and the remote desktop tool.</p><p>(2) Double-click <img loading="lazy" height="71" width="76" src="'+t+'"> to initiate the command-line terminal. Then execute the following command to disable the auto-start service.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.stop_ros.sh</span></span></code></pre></div><p>(3) Open a new command-line terminal <img loading="lazy" height="71" width="76" src="'+t+'">, and run the following command to start the tag sorting.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>roslaunch stepper apriltag_sorting_stepper.launch</span></span></code></pre></div><p>(4) If you need to terminate this program, use short-cut &#39;<strong>Ctrl+C</strong>&#39;.</p><p>(5) After completing the activity, you need to start the APP service <img loading="lazy" height="65" width="77" src="'+p+'"> (if not started, it will affect the use of the APP in subsequent activities). In the terminal, enter the following command and press Enter to start the APP service:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo systemctl start start_app_node.service</span></span></code></pre></div><p>(6) Once the app service has started, the robotic arm will return to its initial position, and the buzzer will emit a &quot;beep&quot; sound.</p><h3 id="_10-5-3-program-outcome" tabindex="-1">10.5.3 Program Outcome <a class="header-anchor" href="#_10-5-3-program-outcome" aria-label="Permalink to &quot;10.5.3 Program Outcome&quot;">​</a></h3><p>After starting the activity, place the tag blocks in the robotic arm&#39;s visual recognition area. The arm will then grab the blocks in the order of tag IDs 1, 2, and 3, and place them at the corresponding locations. We use red, green, and blue colored squares as the placement points for the tag ID 1, 2, and 3 blocks.</p><img loading="lazy" height="706" width="1268" class="common_img" src="'+M+'"><p>If the gripping performance is unsatisfactory, refer to &quot;<a href="https://docs.hiwonder.com/projects/JetArm/en/latest/docs/1.Getting_Ready.html#map-introduction-and-device-placement" target="_blank" rel="noreferrer"><strong>1. Getting Ready(JetArm User Manual)-&gt;1.4.4 Map Introduction and Device Placement</strong></a> and <a href="https://docs.hiwonder.com/projects/JetArm/en/latest/docs/1.Getting_Ready.html#position-calibration" target="_blank" rel="noreferrer"><strong>1. Getting Ready(JetArm User Manual)-&gt;1.4.5 Position Calibration</strong></a> for adjustments. Here, you can only adjust the X and Y coordinates for gripping compensation. If the track has been used, and you need to use the map, the adjustment must be done on the map.</p><h3 id="_10-5-4-package-structure-and-program-analysis" tabindex="-1">10.5.4 Package Structure and Program Analysis <a class="header-anchor" href="#_10-5-4-package-structure-and-program-analysis" aria-label="Permalink to &quot;10.5.4 Package Structure and Program Analysis&quot;">​</a></h3><ul><li><strong>Package Structure Analysis</strong></li></ul><p>When using the tag sorting function, the recognition program package will be called. Its file structure is shown below:</p><img loading="lazy" height="254" width="264" class="common_img" src="'+o+'"><p><code>launch folder</code>: This folder contains the launch files used to start the tag sorting program and initiate the sorting function.</p><p><code>scripts folder</code>: This folder contains the source code files for the tag sorting program, which define the logic functions for the sorting process.</p><p><code>CMakeLists.txt</code>: This file is used for compiling the package and managing related dependencies. It includes the necessary configuration information for starting the ROS services.</p><p><code>package.xml</code>: This is the package description file, used to describe the package&#39;s contents, such as build tools and version numbers.</p><ul><li><strong>Program Analysis</strong></li></ul><p>(1) Launch File Analysis</p><p>The Launch file is saved in : <a href="../_static/source_code/10/stepper_launch.zip">/home/ubuntu/jetarm/src/stepper/launch/apriltag_sorting_stepper.launch</a></p><p>During the execution of the function, the launch file of the current package will be started, as shown in the diagram below:</p>',59),e("p",{"lineno-start":"1"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;launch&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_type&quot; default=&quot;$(optenv CAMERA_TYPE GEMINI)&quot;/&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    &lt;!-- 根据使用的相机设置相应参数(set corresponding parameters based on the used camera) --&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;source_image_topic&quot; if=&quot;$(eval camera_type==&#39;GEMINI&#39;)&quot; default=&quot;/rgbd_cam/color/image_rect_color&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_info_topic&quot; if=&quot;$(eval camera_type==&#39;GEMINI&#39;)&quot; default=&quot;/rgbd_cam/color/camera_info&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;source_image_topic&quot; if=&quot;$(eval camera_type==&#39;USB_CAM&#39;)&quot; default=&quot;/usb_cam/image_rect_color&quot; /&gt;</span></span>
<span class="line"><span>    &lt;arg name=&quot;camera_info_topic&quot; if=&quot;$(eval camera_type==&#39;USB_CAM&#39;)&quot; default=&quot;/usb_cam/camera_info&quot; /&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        &lt;include file=&quot;$(find jetarm_bringup)/launch/base.launch&quot;/&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        &lt;node name=&quot;apriltag_sorting&quot; pkg=&quot;stepper&quot; type=&quot;apriltag_sorting_stepper.py&quot; output=&quot;screen&quot; respawn=&quot;true&quot;&gt;</span></span>
<span class="line"><span>                &lt;param name=&quot;source_image_topic&quot; value=&quot;$(arg source_image_topic)&quot; /&gt;</span></span>
<span class="line"><span>                &lt;param name=&quot;camera_info_topic&quot; value=&quot;$(arg camera_info_topic)&quot; /&gt;</span></span>
<span class="line"><span>        &lt;/node&gt;</span></span>
<span class="line"><span>&lt;/launch&gt;</span></span></code></pre></div><p>As seen in the diagram above, it is necessary to check which type of camera is mounted on the robotic arm (either GEMINI or a USB monocular camera). By identifying the camera type in the current working environment, the corresponding topic information should be updated for subsequent recognition.</p><p>The base.launch file is used to initialize the current state of the robotic arm. It also publishes the node information that controls the movement of the arm&#39;s joints.</p><p>Finally, the tag sorting function is executed by calling the source code file apriltag_sorting_stepper.py.</p><p>(2) Source Code Analysis</p><p>The source code for this program is located at: <a href="../_static/source_code/10/stepper_swiper.zip">jetarm/src/stepper/scripts/apriltag_sorting_stepper.py</a>.</p>`,6),e("p",{"lineno-start":"1"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#!/usr/bin/env python3</span></span>
<span class="line"><span>import os</span></span>
<span class="line"><span>import cv2</span></span>
<span class="line"><span>import rospy</span></span>
<span class="line"><span>import queue</span></span>
<span class="line"><span>import threading</span></span>
<span class="line"><span></span></span>
<span class="line"><span>import numpy as np</span></span>
<span class="line"><span>from vision_utils import fps, xyz_quat_to_mat, xyz_euler_to_mat, pixels_to_world, box_center, mat_to_xyz_euler, distance, extristric_plane_shift</span></span>
<span class="line"><span>from sensor_msgs.msg import Image as RosImage, CameraInfo</span></span>
<span class="line"><span>from std_srvs.srv import Trigger, TriggerRequest, TriggerResponse</span></span>
<span class="line"><span>from std_srvs.srv import SetBool, SetBoolRequest, SetBoolResponse</span></span>
<span class="line"><span></span></span>
<span class="line"><span>from sensor_msgs.msg import Image</span></span>
<span class="line"><span>from vision_utils import fps, draw_tags</span></span>
<span class="line"><span>from hiwonder_interfaces.srv import GetRobotPose</span></span>
<span class="line"><span>from hiwonder_interfaces.msg import MoveAction, MoveGoal, MultiRawIdPosDur</span></span>
<span class="line"><span>from dt_apriltags import Detector</span></span>
<span class="line"><span>from jetarm_sdk import bus_servo_control, pid</span></span>
<span class="line"><span>import actions</span></span>
<span class="line"><span>import actionlib</span></span>
<span class="line"><span>import stepper as Stepper</span></span>
<span class="line"><span>CONFIG_NAME = &#39;/config&#39;</span></span></code></pre></div><p><code>cv2</code>: Used for image processing with OpenCV.</p><p><code>rospy</code>: Used for ROS communication.</p><p><code>numpy</code>: Used for array operations.</p><p><code>queue</code>, <code>threading</code>: Used for multithreading.</p><p>Import necessary modules for coordinate transformation calculations from <code>vision_utils</code>.</p><p>Import service types from <code>std_srvs.srv</code>.</p><p>Import message types from <code>sensor_msgs.msg</code>.</p><p>Import relevant message types and service types from <code>hiwonder_interfaces</code>.</p><p>Import the tag detection functionality from <code>dt_apriltags</code>.</p><p>Import the servo control module from <code>jetarm_sdk</code>.</p><p>Import actions from <code>actions</code>.</p><p>Import the communication library <code>actionlib</code>.</p>`,13),e("p",{"lineno-start":"22"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>import stepper as Stepper</span></span></code></pre></div><p>stepper is used to control the rotation of the stepper motor.</p><p>① Initiate TagTrackingNode Class (Tag Sorting Class)</p><p>In the main function of the executable program, if the node is not shut down, it will continuously call the image_process function within this class. Throughout the program, the TagTrackingNode class is used to initialize parameters and implement the functionality.</p>',4),e("p",{"lineno-start":"316"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if __name__ == &#39;__main__&#39;:</span></span>
<span class="line"><span>    try:</span></span>
<span class="line"><span>        tag_tracking = TagTrackingNode()</span></span>
<span class="line"><span>        rospy.spin()</span></span>
<span class="line"><span>    except Exception as e:</span></span>
<span class="line"><span>        rospy.logerr(str(e))</span></span></code></pre></div><p>② Initialization Function of the <code>TagTrackingNode</code> Class</p><p>The following is a partial screenshot of the initialization function within the <code>TagTrackingNode</code> class:</p>`,3),e("p",{"lineno-start":"38"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>class TagTrackingNode:</span></span>
<span class="line"><span>    def __init__(self):</span></span>
<span class="line"><span>        rospy.init_node(&#39;tag_tracking_node&#39;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.yaw = 500</span></span>
<span class="line"><span>        self.pitch = 350</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.tracker = None</span></span>
<span class="line"><span>        self.enable_select = False</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.at_detector = Detector(searchpath=[&#39;apriltags&#39;], </span></span>
<span class="line"><span>                                    families=&#39;tag36h11&#39;,</span></span>
<span class="line"><span>                                    nthreads=8,</span></span>
<span class="line"><span>                                    quad_decimate=2.0,</span></span>
<span class="line"><span>                                    quad_sigma=0.0,</span></span>
<span class="line"><span>                                    refine_edges=1,</span></span>
<span class="line"><span>                                    decode_sharpening=0.25,</span></span>
<span class="line"><span>                                    debug=0)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.fps = fps.FPS() # 帧率统计器(frame rate counter)</span></span>
<span class="line"><span>        self.servos_pub = rospy.Publisher(&#39;/controllers/multi_id_pos_dur&#39;, MultiRawIdPosDur, queue_size=1)</span></span>
<span class="line"><span>        rospy.sleep(3)</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>        self.lock = threading.RLock()</span></span>
<span class="line"><span>        self.K = None</span></span>
<span class="line"><span>        self.D = None</span></span>
<span class="line"><span>        self.waste_class_name = None</span></span>
<span class="line"><span>        config = rospy.get_param(CONFIG_NAME)</span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span></code></pre></div><p>In the program screenshot above, the tag recognition detector is defined as at_detector. It is used to recognize and differentiate the tag types. The parameters passed to the detector are as follows:</p><p><code>searchpath</code>: The path for recognition.</p><p><code>families</code>: The tag families (types).</p><p><code>nthreads</code>: The number of threads used for recognition.</p><p><code>quad_decimate</code>: Indicates the binary threshold for tag recognition.</p><p><code>quad_sigma</code>: Set to 0 to represent a square shape.</p><p><code>decode_sharpening</code>: The sharpening level for decoding, set to 0.25.</p><p><code>debug</code>: Whether to enable debug mode, with the default setting being 0.</p>`,9),e("p",{"lineno-start":"48"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.at_detector = Detector(searchpath=[&#39;apriltags&#39;], </span></span>
<span class="line"><span>                                    families=&#39;tag36h11&#39;,</span></span>
<span class="line"><span>                                    nthreads=8,</span></span>
<span class="line"><span>                                    quad_decimate=2.0,</span></span>
<span class="line"><span>                                    quad_sigma=0.0,</span></span>
<span class="line"><span>                                    refine_edges=1,</span></span>
<span class="line"><span>                                    decode_sharpening=0.25,</span></span>
<span class="line"><span>                                    debug=0)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"58"},[e("li",null,"A servos_pub publisher is created using rospy.Publisher to publish information.")],-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.servos_pub = rospy.Publisher(&#39;/controllers/multi_id_pos_dur&#39;, MultiRawIdPosDur, queue_size=1)</span></span></code></pre></div><p>A servos_pub publisher is created using rospy.Publisher to publish information.</p><p>The first parameter, <code>&#39;/controllers/multi_id_pos_dur&#39;</code>, specifies the topic name for servo control.</p><p>The second parameter, <code>MultiRawIdPosDur</code>, indicates the message type.</p><p>The third parameter, <code>queue_size=1</code>, sets the size of the message queue.</p>',5),e("ul",{"lineno-start":"62"},[e("li",null,"Initialization of the camera parameters and the acquisition of distortion parameters.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.K = None</span></span>
<span class="line"><span>        self.D = None</span></span>
<span class="line"><span>        self.waste_class_name = None</span></span>
<span class="line"><span>        config = rospy.get_param(CONFIG_NAME)</span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span>
<span class="line"><span>        self.extristric = None</span></span>
<span class="line"><span>        self.roi = None</span></span>
<span class="line"><span>        self.moving_step = 0</span></span>
<span class="line"><span>        self.status = 1</span></span>
<span class="line"><span>        self.tag_size = rospy.get_param(&quot;/config/tag_size&quot;, 0.025)</span></span></code></pre></div><p>K and D represent the camera&#39;s distortion correction coefficients.</p>`,2),e("p",{"lineno-start":"65"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        config = rospy.get_param(CONFIG_NAME)</span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span>
<span class="line"><span>        self.extristric = None</span></span>
<span class="line"><span>        self.roi = None</span></span>
<span class="line"><span>        self.moving_step = 0</span></span>
<span class="line"><span>        self.status = 1</span></span>
<span class="line"><span>        self.tag_size = rospy.get_param(&quot;/config/tag_size&quot;, 0.025)</span></span></code></pre></div><p>The <code>hand2cam_tf_matrix</code> retrieves the current camera distortion parameters, while <code>extrinsic</code> defines the camera&#39;s external parameters for initialization. <code>roi</code> specifies the image area for recognition, and <code>moving_step</code> indicates the gripper status of the robotic arm.</p>`,2),e("ul",{"lineno-start":"86"},[e("li",null,"Initialization of slide rail motor parameters.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>        stepper.go_home(True)</span></span>
<span class="line"><span>        stepper.set_mode(stepper.EN)</span></span>
<span class="line"><span>        stepper.set_div(stepper.DIV_1_4)</span></span>
<span class="line"><span>        self.target = None</span></span>
<span class="line"><span>        self.target_labels = {</span></span>
<span class="line"><span>            &quot;target_1&quot;: False,</span></span>
<span class="line"><span>            &quot;target_2&quot;: False,</span></span>
<span class="line"><span>            &quot;target_3&quot;: False,</span></span>
<span class="line"><span>        }</span></span></code></pre></div><p>As shown in the figure above, the motor control method is set to stepper. The go_home function moves the motor back to the starting position. set_mode defines the movement mode, while set_div sets the stepper motor polarity to DIV_1_4.</p><p>(4) The self.camera_info_sub is defined to receive camera information, using rospy.Subscriber to create a data subscriber.</p>`,3),e("p",{"lineno-start":"98"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        camera_info_topic = rospy.get_param(&#39;~camera_info_topic&#39;, &#39;/camera/camera_info&#39;)</span></span>
<span class="line"><span>        self.camera_info_sub = rospy.Subscriber(camera_info_topic, CameraInfo, self.camera_info_callback, queue_size=1) #订阅相机内参(subscribe camera intrinsic parameter)</span></span></code></pre></div><p>The first parameter, <code>camera_info_topic</code>, specifies the topic name for receiving image data.</p><p>The second parameter, <code>CameraInfo</code>, is the message type.</p><p>The third parameter specifies the callback function, <code>self.camera_info_callback</code>, which processes the received images.</p><p>The fourth parameter, <code>queue_size=1</code>, sets the size of the message queue.</p><p>③ Calculate the world coordinates of the four corners of the recognized area.</p>`,6),e("p",{"lineno-start":"102"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>         # 识别区域的四个角的世界坐标(the world coordinates of four corners in recognition region)</span></span>
<span class="line"><span>        white_area_cam = config[&#39;white_area_pose_cam&#39;]</span></span>
<span class="line"><span>        white_area_center = config[&#39;white_area_pose_world&#39;]</span></span>
<span class="line"><span>        self.white_area_center = white_area_center</span></span>
<span class="line"><span>        self.white_area_cam = white_area_cam</span></span>
<span class="line"><span>        white_area_height = config[&#39;white_area_world_size&#39;][&#39;height&#39;]</span></span>
<span class="line"><span>        white_area_width = config[&#39;white_area_world_size&#39;][&#39;width&#39;]</span></span>
<span class="line"><span>        white_area_lt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_lb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        self.get_endpoint()</span></span>
<span class="line"><span>        corners_cam =  np.matmul(np.linalg.inv(np.matmul(self.endpoint, config[&#39;hand2cam_tf_matrix&#39;])), [white_area_lt, white_area_lb, white_area_rb, white_area_rt, white_area_center])</span></span>
<span class="line"><span>        corners_cam = np.matmul(np.linalg.inv(white_area_cam), corners_cam)</span></span>
<span class="line"><span>        corners_cam = corners_cam[:, :3, 3:].reshape((-1, 3))</span></span>
<span class="line"><span>        tvec, rmat = config[&#39;extristric&#39;]</span></span>
<span class="line"><span></span></span>
<span class="line"><span></span></span>
<span class="line"><span>        while self.K is None or self.D is None: # 等待获取相机内参(wait for obtaining camera intrinsic parameters)</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span>
<span class="line"><span>        center_imgpts, jac = cv2.projectPoints(corners_cam[-1:], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.center_imgpts = np.int32(center_imgpts).reshape(2)</span></span>
<span class="line"><span>        tvec, rmat = extristric_plane_shift(np.array(tvec).reshape((3, 1)), np.array(rmat), 0.04)</span></span>
<span class="line"><span>        self.extristric = tvec, rmat</span></span>
<span class="line"><span>        imgpts, jac = cv2.projectPoints(corners_cam[:-1], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.imgpts = np.int32(imgpts).reshape(-1, 2)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"103"},[e("li",null,"Obtain the camera and world coordinates of the white area and store them as class attributes.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_cam = config[&#39;white_area_pose_cam&#39;]</span></span>
<span class="line"><span>        white_area_center = config[&#39;white_area_pose_world&#39;]</span></span>
<span class="line"><span>        self.white_area_center = white_area_center</span></span>
<span class="line"><span>        self.white_area_cam = white_area_cam</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"107"},[e("li",null,"Get the height and width of the white area.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_height = config[&#39;white_area_world_size&#39;][&#39;height&#39;]</span></span>
<span class="line"><span>        white_area_width = config[&#39;white_area_world_size&#39;][&#39;width&#39;]</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"109"},[e("li",null,[s("Use the "),e("code",null,"xyz_euler_to_mat"),s(" function to transform the four corner points of the white area from the world coordinate system to the camera coordinate system.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        white_area_lt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_lb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rb = np.matmul(white_area_center, xyz_euler_to_mat((-white_area_height / 2 - 0.01, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span>
<span class="line"><span>        white_area_rt = np.matmul(white_area_center, xyz_euler_to_mat((white_area_height / 2, -white_area_width / 2, 0.0), (0, 0, 0)))</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"113"},[e("li",null,[s("Call the "),e("code",null,"self.get_endpoint()"),s(" function to get the current pose of the end effector. Then, use matrix operations to transform the corner points of the white area from the camera coordinate system to the world coordinate system and store them in "),e("code",null,"corners_cam"),s(". Retrieve the extrinsic parameters and assign them to "),e("code",null,"tvec"),s(" and "),e("code",null,"rmat"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.get_endpoint()</span></span>
<span class="line"><span>        corners_cam =  np.matmul(np.linalg.inv(np.matmul(self.endpoint, config[&#39;hand2cam_tf_matrix&#39;])), [white_area_lt, white_area_lb, white_area_rb, white_area_rt, white_area_center])</span></span>
<span class="line"><span>        corners_cam = np.matmul(np.linalg.inv(white_area_cam), corners_cam)</span></span>
<span class="line"><span>        corners_cam = corners_cam[:, :3, 3:].reshape((-1, 3))</span></span>
<span class="line"><span>        tvec, rmat = config[&#39;extristric&#39;]</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"120"},[e("li",null,"Wait for the camera intrinsic parameters.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        while self.K is None or self.D is None: # 等待获取相机内参(wait for obtaining camera intrinsic parameters)</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"123"},[e("li",null,"Obtain the transformation matrix from hand to camera coordinates.")],-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        self.hand2cam_tf_matrix = config[&#39;hand2cam_tf_matrix&#39;]</span></span></code></pre></div>',1),e("ul",{"lineno-start":"124"},[e("li",null,[s("Project the last corner point ("),e("code",null,"corners_cam[-1:]"),s(") from the camera coordinate system to the image plane to get the center point coordinates ("),e("code",null,"center_imgpts"),s(") on the image.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        center_imgpts, jac = cv2.projectPoints(corners_cam[-1:], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.center_imgpts = np.int32(center_imgpts).reshape(2)</span></span></code></pre></div><p>The projection uses the camera intrinsic parameters <code>self.K</code> and <code>self.D</code>, along with the extrinsic parameters <code>rmat</code> and <code>tvec</code>. The projected center point image coordinates are stored in <code>self.center_imgpts</code>.</p>`,2),e("ul",{"lineno-start":"126"},[e("li",null,[s("Call the "),e("code",null,"extristric_plane_shift"),s(" function to fine-tune the translation vector and rotation matrix. The adjusted translation vector and rotation matrix are stored in "),e("code",null,"self.extristric"),s(".")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        tvec, rmat = extristric_plane_shift(np.array(tvec).reshape((3, 1)), np.array(rmat), 0.04)</span></span>
<span class="line"><span>        self.extristric = tvec, rmat</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"128"},[e("li",null,"Project the remaining corner points (i.e., all except the last one, corners_cam[:-1]) from the camera coordinate system to the image plane to obtain a set of image coordinates, imgpts. The projected image coordinates are stored in self.imgpts.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        imgpts, jac = cv2.projectPoints(corners_cam[:-1], np.array(rmat), np.array(tvec), self.K, self.D)</span></span>
<span class="line"><span>        self.imgpts = np.int32(imgpts).reshape(-1, 2)</span></span></code></pre></div><p>④ Calculate the ROI Area</p><p>Obtain the maximum and minimum values for the x and y axes in the image, and use np.maximum to compare these values with 0, resulting in a non-negative array roi.</p>`,3),e("p",{"lineno-start":"132"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        x_min = min(self.imgpts, key=lambda p: p[0])[0]  # x轴最小值(the minimum value of X-axis)</span></span>
<span class="line"><span>        x_max = max(self.imgpts, key=lambda p: p[0])[0]  # x轴最大值(the maximum value of X-axis)</span></span>
<span class="line"><span>        y_min = min(self.imgpts, key=lambda p: p[1])[1]  # y轴最小值(the minimum value of Y-axis)</span></span>
<span class="line"><span>        y_max = max(self.imgpts, key=lambda p: p[1])[1]  # y轴最大值(the maximum value of Y-axis)</span></span>
<span class="line"><span>        roi = np.maximum(np.array([y_min, y_max, x_min, x_max]), 0)</span></span>
<span class="line"><span>        self.roi = roi</span></span></code></pre></div><p>⑤ Create an action group client and an image information subscriber</p><p>A simple action client named action_client is created to publish movement actions to the /grasp topic.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        source_image_topic = rospy.get_param(&#39;~source_image_topic&#39;, &#39;/camera/image_raw&#39;)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;订阅原图像节点 &quot; + source_image_topic)</span></span>
<span class="line"><span>        self.action_client = actionlib.SimpleActionClient(&#39;/grasp&#39;, MoveAction)</span></span>
<span class="line"><span>        self.image_sub = rospy.Subscriber(source_image_topic, RosImage, self.image_callback, queue_size=1)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;启动完成\\r\\n\\r\\n&quot;)</span></span></code></pre></div><p>The image topic name is retrieved from the ROS parameter server, defaulting to /camera/image_raw, and a subscriber is created to listen to this image topic.</p><p>A message is logged using rospy.loginfo to indicate that the node has successfully started.</p><p>⑥ camera_info_callback Function</p><p>The camera intrinsic matrix is extracted from the received camera information message and stored in the class member variable self.K.</p>`,8),e("p",{"lineno-start":"146"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def camera_info_callback(self, msg): # 相机内参回调(callback camera intrinsic parameters)</span></span>
<span class="line"><span>        with self.lock:</span></span>
<span class="line"><span>            K = np.matrix(msg.K).reshape(1, -1, 3)</span></span>
<span class="line"><span>            D = np.array(msg.D)</span></span>
<span class="line"><span>            new_K, roi = cv2.getOptimalNewCameraMatrix(K, D, (640, 480), 0, (640, 480))</span></span>
<span class="line"><span>            self.K, self.D = np.matrix(new_K), np.zeros((5, 1))</span></span></code></pre></div><p><code>msg.K</code>: Represents the camera intrinsic matrix in the camera information message.</p><p><code>np.matrix(msg.K)</code>: Converts the camera intrinsic matrix into a NumPy matrix.</p><p><code>.reshape(1, -1, 3)</code>: Reshapes the matrix into a 1xN format, where N is the number of elements in the matrix, and each element contains 3 values (e.g., row, column, focal length).</p><p><code>self.D = np.array(msg.D)</code>: Converts the distortion coefficients \`D\` from the received camera information into a NumPy array.</p><p>⑦ get_endpoint Function</p><p>Used to obtain the pose information of the robot&#39;s end effector.</p>`,7),e("p",{"lineno-start":"153"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def get_endpoint(self):</span></span>
<span class="line"><span>        endpoint = rospy.ServiceProxy(&#39;/kinematics/get_current_pose&#39;, GetRobotPose)().pose</span></span>
<span class="line"><span>        self.endpoint = xyz_quat_to_mat([endpoint.position.x, endpoint.position.y, endpoint.position.z],</span></span>
<span class="line"><span>                                        [endpoint.orientation.w, endpoint.orientation.x, endpoint.orientation.y, endpoint.orientation.z])</span></span></code></pre></div><p><code>endpoint</code>: Uses a ROS service proxy to call the <code>/kinematics/get_current_pose</code> service to retrieve the robot&#39;s current pose information, which is then stored in the <code>endpoint</code> variable.</p><p>Calls the <code>xyz_quat_to_mat</code> function to convert the retrieved pose information into a pose matrix, and stores the result in the <code>self.endpoint</code> attribute.</p><p>⑧ done_callback Function</p>`,4),e("ul",{"lineno-start":"158"},[e("li",null,"This function is a callback that is triggered when an action is completed. Based on the action's status and result, it performs a series of steps to handle different situations.")],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def done_callback(self, state, result):  # 动作执行完毕回调(callback for action execution completion)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;state:%f&quot;, state)</span></span>
<span class="line"><span>        if not result.result.complete:  # 如果在移动中被取消，需要回到初始位置(If the action is cancelled during movement, return to the initial position)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((1, 500), ))</span></span>
<span class="line"><span>            rospy.sleep(1)</span></span>
<span class="line"><span>            actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((10, 200), ))</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span>
<span class="line"><span>        elif self.moving_step != 1:</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((10, 200), ))</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        if self.finish_percent == 1:  # 如果完整的完成移动(if the movement is completed in full)</span></span>
<span class="line"><span>            if self.moving_step == 1:  # 如果完成了夹取(if the gripper has completed the grasping)</span></span>
<span class="line"><span>                actions.go_back(self.servos_pub)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                self.moving_step = 2</span></span>
<span class="line"><span>                goal = MoveGoal()</span></span>
<span class="line"><span>                goal.grasp.mode = &#39;place&#39;</span></span>
<span class="line"><span>                rospy.sleep(1)</span></span>
<span class="line"><span>               </span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[&#39;target_&#39; + str(self.target)]</span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 设置滑轨使能(enable the slider track)</span></span>
<span class="line"><span>                stepper.goto(stepper_position) # 驱动滑轨移动到放置位置(drive the slider track to move to the placing position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000 # 计算需要的时间(calculate the required time)</span></span>
<span class="line"><span>                rospy.sleep(stepper_time)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                target_position = TARGET_POSITION_STEPPER[&#39;target_&#39;+ str(self.target)]# 读取放置坐标(read placing coordinate)</span></span>
<span class="line"><span>                goal.grasp.position.x = target_position[0]</span></span>
<span class="line"><span>                goal.grasp.position.y = target_position[1]</span></span>
<span class="line"><span>                goal.grasp.position.z = target_position[2]</span></span>
<span class="line"><span>                goal.grasp.pitch = self.pick_pitch</span></span>
<span class="line"><span>                goal.grasp.grasp_approach.z = 0.06  # 放置时靠近的方向和距离(the direction and distance for approaching during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_retreat.z = 0.06  # 放置后后撤的方向和距离(the direction and distance of retreat during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_posture = 370  # 夹取前后夹持器的开合角度(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>                goal.grasp.pre_grasp_posture = 550</span></span>
<span class="line"><span>                self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"158"},[e("li",null,[e("p",null,[s("If the movement is canceled, the robot arm will call "),e("code",null,"set_servos"),s(" from "),e("code",null,"bus_servo_control"),s(" to return the gimbal to its initial position. Then, it will call "),e("code",null,"go_home"),s(" from the actions module to return the robot arm to its starting position. Additionally, the "),e("code",null,"stepper"),s(" controls the rail motor to continue moving the arm on the platform to the specified position.")])]),e("li",null,[e("p",null,[s("If the grasping process is canceled, the robot arm will call "),e("code",null,"set_servos"),s(" from "),e("code",null,"bus_servo_control"),s(" to release the gripper.")])])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def done_callback(self, state, result):  # 动作执行完毕回调(callback for action execution completion)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;state:%f&quot;, state)</span></span>
<span class="line"><span>        if not result.result.complete:  # 如果在移动中被取消，需要回到初始位置(If the action is cancelled during movement, return to the initial position)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((1, 500), ))</span></span>
<span class="line"><span>            rospy.sleep(1)</span></span>
<span class="line"><span>            actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>            bus_servo_control.set_servos(self.servos_pub, 500, ((10, 200), ))</span></span>
<span class="line"><span>            rospy.sleep(0.5)</span></span></code></pre></div><ul><li>If the movement and grasping are completed successfully, the gripper mode is set to &#39;place&#39;. The target position information is retrieved from <code>target_position</code>, along with the alignment angle, approach and retreat distances during grasping, and the opening and closing angles of the gripper. Finally, the goal is sent to the action client via <code>self.action_client.send_goal</code>.</li></ul><p><code>self.action_client</code> is an Action Client object.</p><p><code>send_goal()</code> is the method used to send the goal to the server.</p><p><code>goal</code> is the target to be sent.</p><p><code>self.done_callback</code> is the callback function called when the goal is completed.</p><p><code>self.active_callback</code> is the callback function called when the goal is accepted and execution begins.</p><p><code>self.feedback_callback</code> is the callback function called when feedback is provided by the server.</p>`,8),e("p",{"lineno-start":"169"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        if self.finish_percent == 1:  # 如果完整的完成移动(if the movement is completed in full)</span></span>
<span class="line"><span>            if self.moving_step == 1:  # 如果完成了夹取(if the gripper has completed the grasping)</span></span>
<span class="line"><span>                actions.go_back(self.servos_pub)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                self.moving_step = 2</span></span>
<span class="line"><span>                goal = MoveGoal()</span></span>
<span class="line"><span>                goal.grasp.mode = &#39;place&#39;</span></span>
<span class="line"><span>                rospy.sleep(1)</span></span>
<span class="line"><span>               </span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[&#39;target_&#39; + str(self.target)]</span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 设置滑轨使能(enable the slider track)</span></span>
<span class="line"><span>                stepper.goto(stepper_position) # 驱动滑轨移动到放置位置(drive the slider track to move to the placing position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000 # 计算需要的时间(calculate the required time)</span></span>
<span class="line"><span>                rospy.sleep(stepper_time)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                target_position = TARGET_POSITION_STEPPER[&#39;target_&#39;+ str(self.target)]# 读取放置坐标(read placing coordinate)</span></span>
<span class="line"><span>                goal.grasp.position.x = target_position[0]</span></span>
<span class="line"><span>                goal.grasp.position.y = target_position[1]</span></span>
<span class="line"><span>                goal.grasp.position.z = target_position[2]</span></span>
<span class="line"><span>                goal.grasp.pitch = self.pick_pitch</span></span>
<span class="line"><span>                goal.grasp.grasp_approach.z = 0.06  # 放置时靠近的方向和距离(the direction and distance for approaching during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_retreat.z = 0.06  # 放置后后撤的方向和距离(the direction and distance of retreat during placing)</span></span>
<span class="line"><span>                goal.grasp.grasp_posture = 370  # 夹取前后夹持器的开合角度(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>                goal.grasp.pre_grasp_posture = 550</span></span>
<span class="line"><span>                self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"196"},[e("li",null,[s("If the grasping of the tag block is completed, the "),e("code",null,"go_home"),s(" function from the actions module is called to return the robot arm to the initial position, reset related variables, and the "),e("code",null,"stepper"),s(" is used to control the rail motor to move the robot arm to the specified position on the platform.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>            elif self.moving_step == 2:  # 如果完成了放置(if the placement is completed)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                #actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>                rospy.sleep(1.5)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                stepper = Stepper.Stepper(7)</span></span>
<span class="line"><span>                stepper_position = TARGET_POSITION[&#39;target_&#39; + str(self.target)]</span></span>
<span class="line"><span>                stepper.goto(-stepper_position) # 滑轨回到初始位置(the slider track returns to the initial position)</span></span>
<span class="line"><span>                stepper_time = stepper_position/1000</span></span>
<span class="line"><span>                rospy.sleep(stepper_time)</span></span>
<span class="line"><span>                stepper.set_mode(stepper.EN) # 解除滑轨锁定(release slider track lock)</span></span>
<span class="line"><span>                print(&quot;FINISHED&quot;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                self.get_endpoint()</span></span>
<span class="line"><span>                self.last_position = None</span></span>
<span class="line"><span>                self.target = None</span></span>
<span class="line"><span>                self.count = 0</span></span>
<span class="line"><span>                self.moving_step = 0</span></span></code></pre></div>`,1),e("ul",{"lineno-start":"214"},[e("li",null,[s("If the action is canceled or the specified position cannot be reached, "),e("code",null,"go_home"),s(" from the actions module is called to return the robot arm to the initial position, and related variables are reset.")])],-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        else:  # 如果被取消或者无法到达指定位置(if the action is cancelled, or it is unable to reach the specified position)</span></span>
<span class="line"><span>            actions.go_home(self.servos_pub)</span></span>
<span class="line"><span>            self.moving_step = 0</span></span>
<span class="line"><span>            self.last_position = None</span></span></code></pre></div><p>⑨ active_callback Function</p><p>Set a variable named start_move to True at the beginning of the movement and log a message indicating that the movement has started.</p>`,3),e("p",{"lineno-start":"219"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def active_callback(self):  # 运动开始回调(callback for starting the movement)</span></span>
<span class="line"><span>        self.start_move = True</span></span>
<span class="line"><span>        rospy.loginfo(&quot;start move&quot;)</span></span></code></pre></div><p>⑩ feedback_callback Function</p><p>The main function of this callback is to retrieve the action execution progress and print this information to the log.</p>`,3),e("p",{"lineno-start":"223"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def feedback_callback(self, msg):  # 动作执行进度回调(callback action execution progress)</span></span>
<span class="line"><span>        rospy.loginfo(&quot;finish action: {:.2%}&quot;.format(msg.percent))</span></span>
<span class="line"><span>        self.finish_percent = msg.percent</span></span></code></pre></div><p>⑪ start_moving Function</p><p>Set the relevant parameters for the robot arm&#39;s handling and stacking tasks by creating a MoveGoal target object and assigning these parameters to its attributes. These parameters include the target position, orientation angles, and gripping method, among others. The action client then sends this information to the robot arm control node to initiate the corresponding handling and stacking actions.</p>`,3),e("p",{"lineno-start":"227"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def start_moving(self, pose_t, pose_R):</span></span>
<span class="line"><span>        rospy.loginfo(&quot;开始搬运堆叠...&quot;)</span></span>
<span class="line"><span>        #print(pose_t, pose_R)</span></span>
<span class="line"><span>        self.moving_step = 1</span></span>
<span class="line"><span>        goal = MoveGoal()</span></span>
<span class="line"><span>        goal.grasp.mode = &#39;pick&#39;</span></span>
<span class="line"><span>        # 物体坐标(object coordinate)</span></span>
<span class="line"><span>        goal.grasp.position.x =  pose_t[0]</span></span>
<span class="line"><span>        goal.grasp.position.y =  pose_t[1]</span></span>
<span class="line"><span>        goal.grasp.position.z = -0.033</span></span>
<span class="line"><span>        # 夹取时的姿态角(the posture angle during grasping)</span></span>
<span class="line"><span>        goal.grasp.pitch = self.pick_pitch</span></span>
<span class="line"><span>        goal.grasp.align_angle = 0  # 总是朝前(always moving forward)</span></span>
<span class="line"><span>        # 夹取时靠近的方向和距离(the direction and distance for approaching during grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_approach.z = 0.03</span></span>
<span class="line"><span>        # 夹取后后撤方向和距离(the direction and distance of retreat during grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_retreat.z = 0.05</span></span>
<span class="line"><span>        # 夹取前后夹持器的开合(the opening and closing of the gripper before and after the grasping)</span></span>
<span class="line"><span>        goal.grasp.grasp_posture = 580</span></span>
<span class="line"><span>        goal.grasp.pre_grasp_posture = 220</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        self.action_client.send_goal(goal, self.done_callback, self.active_callback, self.feedback_callback)  # 发送夹取请求(send grasping request)</span></span>
<span class="line"><span>        rospy.sleep(3)</span></span></code></pre></div><p>⑫ image_callback Function</p>`,2),e("p",{"lineno-start":"261"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>    def image_callback(self, ros_image):</span></span>
<span class="line"><span>        #rospy.logdebug(&#39;Received an image! &#39;)</span></span>
<span class="line"><span>        # 将画面转为 opencv 格式(convert the image to opencv format)</span></span>
<span class="line"><span>        rgb_image = np.ndarray(shape=(ros_image.height, ros_image.width, 3), dtype=np.uint8, buffer=ros_image.data)</span></span>
<span class="line"><span>        result_image = np.copy(rgb_image)</span></span>
<span class="line"><span>        try:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>            if self.moving_step == 0 and self.roi is not None and self.K is not None and self.D is not None:</span></span>
<span class="line"><span>                roi_area_mask = np.zeros(shape=(ros_image.height, ros_image.width, 1), dtype=np.uint8)</span></span>
<span class="line"><span>                roi_area_mask = cv2.drawContours(roi_area_mask, [self.imgpts], -1, 255, cv2.FILLED)</span></span>
<span class="line"><span>                rgb_image = cv2.bitwise_and(rgb_image, rgb_image, mask=roi_area_mask)  # 和原图做遮罩，保留需要识别的区域(create a mask based on the original image to retain the region to be recognized)</span></span>
<span class="line"><span>                tags = self.at_detector.detect(cv2.cvtColor(rgb_image, cv2.COLOR_RGB2GRAY), True, (self.K[0,0], self.K[1,1], self.K[0,2], self.K[1,2]), self.tag_size)</span></span>
<span class="line"><span>                tags = sorted(tags, key=lambda tag: tag.tag_id) # 貌似出来就是升序排列的不需要手动进行排列(It seems to be sorted in ascending order automatically and does not need to be manually sorted)</span></span>
<span class="line"><span>                draw_tags(result_image, tags, corners_color=(0, 0, 255), center_color=(0, 255, 0))</span></span>
<span class="line"><span>                if len(tags) &gt; 0:  </span></span>
<span class="line"><span>                    projection_matrix = np.row_stack((np.column_stack((self.extristric[1], self.extristric[0])), np.array([[0, 0, 0, 1]])))</span></span>
<span class="line"><span>                    world_pose = pixels_to_world([tags[0].center, ], self.K, projection_matrix)[0]  # 像素坐标相对于识别区域中心的相对坐标(the relative coordinate of the pixel coordinate with respect to the center of the recognition region)</span></span>
<span class="line"><span>                    world_pose[1] = -world_pose[1]</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span>
<span class="line"><span>                    world_pose = np.matmul(self.white_area_center, xyz_euler_to_mat(world_pose, (0, 0, 0)))  # 转换到相机相对坐标(convert to the relative coordinate of the camera)</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span>
<span class="line"><span>                    </span></span>
<span class="line"><span>                    pose_t, pose_R = mat_to_xyz_euler(world_pose)</span></span>
<span class="line"><span>                    params = rospy.get_param(os.path.join(POSITIONS_PATH, &#39;tag_sortting&#39;))</span></span>
<span class="line"><span>                            </span></span>
<span class="line"><span>                    for i in range(3):</span></span>
<span class="line"><span>                        pose_t[i] = pose_t[i] + params[&#39;offset&#39;][i]</span></span>
<span class="line"><span>                        pose_t[i] = pose_t[i] * params[&#39;scale&#39;][i]</span></span>
<span class="line"><span>                       # print(pose_t)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                    self.target = tags[0].tag_id</span></span>
<span class="line"><span>                    #print(&quot;tags[0].tag_id=&quot;,tags[0].tag_id)</span></span>
<span class="line"><span>                    if self.target in [1, 2, 3]:</span></span>
<span class="line"><span>                        self.count += 1</span></span>
<span class="line"><span>                        if self.count &gt; 40:</span></span>
<span class="line"><span>                            self.moving_step = 1</span></span>
<span class="line"><span>                            self.status = 1</span></span>
<span class="line"><span>                            threading.Thread(target=self.action_starting, args=(pose_t, pose_R)).start()</span></span></code></pre></div><p>The function receives an image as a parameter, <code>ros_image</code>, of type <code>RosImage</code>, which represents the incoming image message. This image needs to be converted into OpenCV format.</p>`,2),e("p",{"lineno-start":"264"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        rgb_image = np.ndarray(shape=(ros_image.height, ros_image.width, 3), dtype=np.uint8, buffer=ros_image.data)</span></span>
<span class="line"><span>        result_image = np.copy(rgb_image)</span></span></code></pre></div><p>The current status of the robot arm is checked. If the current state (<code>move_step</code>) is set to the initial value (0), and the ROI, distortion coefficients K and D are not empty, the ROI of the new image is set. The specific steps are outlined below, and the final image used for recognition is <code>rgb_image</code>.</p>`,2),e("p",{"lineno-start":"268"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>            if self.moving_step == 0 and self.roi is not None and self.K is not None and self.D is not None:</span></span>
<span class="line"><span>                roi_area_mask = np.zeros(shape=(ros_image.height, ros_image.width, 1), dtype=np.uint8)</span></span>
<span class="line"><span>                roi_area_mask = cv2.drawContours(roi_area_mask, [self.imgpts], -1, 255, cv2.FILLED)</span></span>
<span class="line"><span>                rgb_image = cv2.bitwise_and(rgb_image, rgb_image, mask=roi_area_mask)  # 和原图做遮罩，保留需要识别的区域(create a mask based on the original image to retain the region to be recognized)</span></span></code></pre></div><p>The <code>tags</code> represent the recognized label contents. In the <code>detect</code> function, the input image is <code>rgb_image</code>, which is converted to grayscale (<code>GRAY</code>). The previously saved distortion coefficients are loaded, and the actual label size (<code>self.tag_size</code>) is passed in.</p>`,2),e("p",{"lineno-start":"272"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                tags = self.at_detector.detect(cv2.cvtColor(rgb_image, cv2.COLOR_RGB2GRAY), True, (self.K[0,0], self.K[1,1], self.K[0,2], self.K[1,2]), self.tag_size)</span></span></code></pre></div><p>The recognized label results are sorted.</p>',2),e("p",{"lineno-start":"273"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                tags = sorted(tags, key=lambda tag: tag.tag_id) # 貌似出来就是升序排列的不需要手动进行排列(It seems to be sorted in ascending order automatically and does not need to be manually sorted)</span></span></code></pre></div><p>A library function is called to return the recognized label results.</p>',2),e("p",{"lineno-start":"274"},null,-1),a('<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                draw_tags(result_image, tags, corners_color=(0, 0, 255), center_color=(0, 255, 0))</span></span></code></pre></div><p>Once a label is detected, its current position must be determined. After identifying the position, the robot arm can be controlled to grasp the label.</p>',2),e("p",{"lineno-start":"275"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                if len(tags) &gt; 0:  </span></span>
<span class="line"><span>                    projection_matrix = np.row_stack((np.column_stack((self.extristric[1], self.extristric[0])), np.array([[0, 0, 0, 1]])))</span></span>
<span class="line"><span>                    world_pose = pixels_to_world([tags[0].center, ], self.K, projection_matrix)[0]  # 像素坐标相对于识别区域中心的相对坐标(the relative coordinate of the pixel coordinate with respect to the center of the recognition region)</span></span>
<span class="line"><span>                    world_pose[1] = -world_pose[1]</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span>
<span class="line"><span>                    world_pose = np.matmul(self.white_area_center, xyz_euler_to_mat(world_pose, (0, 0, 0)))  # 转换到相机相对坐标(convert to the relative coordinate of the camera)</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span>
<span class="line"><span>                    </span></span>
<span class="line"><span>                    pose_t, pose_R = mat_to_xyz_euler(world_pose)</span></span>
<span class="line"><span>                    params = rospy.get_param(os.path.join(POSITIONS_PATH, &#39;tag_sortting&#39;))</span></span>
<span class="line"><span>                            </span></span>
<span class="line"><span>                    for i in range(3):</span></span>
<span class="line"><span>                        pose_t[i] = pose_t[i] + params[&#39;offset&#39;][i]</span></span>
<span class="line"><span>                        pose_t[i] = pose_t[i] * params[&#39;scale&#39;][i]</span></span>
<span class="line"><span>                       # print(pose_t)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                    self.target = tags[0].tag_id</span></span>
<span class="line"><span>                    #print(&quot;tags[0].tag_id=&quot;,tags[0].tag_id)</span></span>
<span class="line"><span>                    if self.target in [1, 2, 3]:</span></span>
<span class="line"><span>                        self.count += 1</span></span>
<span class="line"><span>                        if self.count &gt; 40:</span></span>
<span class="line"><span>                            self.moving_step = 1</span></span>
<span class="line"><span>                            self.status = 1</span></span>
<span class="line"><span>                            threading.Thread(target=self.action_starting, args=(pose_t, pose_R)).start()</span></span></code></pre></div><p>During the grasping process, the image coordinates of the detected label are transformed from the camera coordinate system to the world coordinate system. The XYZ axis data from the world coordinate system is then mapped to the camera coordinate system, yielding the relative position of the camera with respect to the label. The steps for this are shown in the code snippet below:</p>`,2),e("p",{"lineno-start":"276"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                    projection_matrix = np.row_stack((np.column_stack((self.extristric[1], self.extristric[0])), np.array([[0, 0, 0, 1]])))</span></span>
<span class="line"><span>                    world_pose = pixels_to_world([tags[0].center, ], self.K, projection_matrix)[0]  # 像素坐标相对于识别区域中心的相对坐标(the relative coordinate of the pixel coordinate with respect to the center of the recognition region)</span></span>
<span class="line"><span>                    world_pose[1] = -world_pose[1]</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span>
<span class="line"><span>                    world_pose = np.matmul(self.white_area_center, xyz_euler_to_mat(world_pose, (0, 0, 0)))  # 转换到相机相对坐标(convert to the relative coordinate of the camera)</span></span>
<span class="line"><span>                    world_pose[2] = 0.04</span></span></code></pre></div><p>Based on the sorted labels, the <code>start_moving</code> function is called to grasp the corresponding labeled blocks.</p>`,2),e("p",{"lineno-start":"291"},null,-1),a(`<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                    self.target = tags[0].tag_id</span></span>
<span class="line"><span>                    #print(&quot;tags[0].tag_id=&quot;,tags[0].tag_id)</span></span>
<span class="line"><span>                    if self.target in [1, 2, 3]:</span></span>
<span class="line"><span>                        self.count += 1</span></span>
<span class="line"><span>                        if self.count &gt; 40:</span></span>
<span class="line"><span>                            self.moving_step = 1</span></span>
<span class="line"><span>                            self.status = 1</span></span>
<span class="line"><span>                            threading.Thread(target=self.action_starting, args=(pose_t, pose_R)).start()</span></span></code></pre></div><h3 id="_10-5-5-function-extension" tabindex="-1">10.5.5 Function Extension <a class="header-anchor" href="#_10-5-5-function-extension" aria-label="Permalink to &quot;10.5.5 Function Extension&quot;">​</a></h3><p>When adjusting the position of the sliding rail and the placement of the label blocks on the robot arm, you can follow the steps below to modify the parameters:</p><p>(1) Click on the &quot;<strong>File</strong>&quot; icon in the system taskbar (on the left) to open the file explorer.</p><img loading="lazy" height="288" width="61" class="common_img" src="`+l+'"><p>(2) Navigate to the package folder and double-click to open the <code>apriltag_sorting_stepper.py</code> file.</p><img loading="lazy" height="285" width="691" class="common_img" src="'+B+'"><p>(3) As shown in the image below, the area highlighted in green represents the parameters for the label card placement, while the red box highlights the parameters for the sliding rail movement.</p><img loading="lazy" height="377" width="581" class="common_img" src="'+G+'"><p>(4) You can then refer to the following two points to adjust the target placement position and the movement position of the sliding rail platform.</p><ul><li><strong>Changing the Target Placement Position</strong></li></ul><p>(1) As shown in the diagram, the TARGET_POSITION_STEPPER dictionary defines three keys: target_1, target_2, and target_3. Each key corresponds to a different value, and each value contains three parameters.</p><img loading="lazy" height="91" width="365" class="common_img" src="'+L+'"><p>(2) Taking target_1 as an example, the values [0.25, 0, -0.023] represent the relative position on the robot&#39;s three axes (X, Y, Z). These values indicate the position relative to the robot&#39;s X, Y, and Z axes, with the unit in meters. The specific positions for the three axes can be referred to in the image below.</p><img loading="lazy" height="673" width="956" class="common_img" src="'+n+'"><p>(3) If the placement position is closer to the center along the X-axis, you can slightly increase the relative value of the X-axis for target_1. For example, changing 0.25 to 0.26 will place the target 1 centimeter further along the X-axis after the modification.</p><p>(4) It&#39;s important not to make the changes too large to ensure the robot arm has a safe movement space. Adjust the values according to the required shift. Similarly, the Y-axis and Z-axis values indicate the placement positions in the horizontal direction (Y-axis) and vertical height (Z-axis), respectively. You can modify these values as needed, keeping in mind that all modifications should be made in meters (m).</p><p>(5) The placement values for the other two targets, target_2 and target_3, follow the same process as target_1. You can adjust them using the same steps as those for modifying target_1.</p><ul><li><strong>Changing the Sliding Platform Movement Position</strong></li></ul><p>(1) To adjust the movement position of the sliding platform, you can modify the program content shown in the diagram below.</p><img loading="lazy" height="98" width="331" class="common_img" src="'+F+'"><p>(2) In the defined <code>TARGET_POSITION</code> dictionary, there are three keys: <code>target_1</code>, <code>target_2</code>, and <code>target_3</code>. Each key corresponds to a different value, which contains three parameters representing the movement distance of the sliding platform. These values can be found in the program screenshot above, where each of the three target objects corresponds to a different placement position.</p><img loading="lazy" height="322" width="914" class="common_img" src="'+r+'"><p>(3) In the diagram, the robot&#39;s starting position is 0 (the minimum value), and the maximum value is 5200. By changing the position value after each <code>target</code>, you can adjust the movement position of the sliding platform. For example, if the value for <code>target_1</code> is set to 2600, setting it to 2000 will move the platform to position 2000. The same applies to <code>target_2</code> and <code>target_3</code>; simply modify the position value after each <code>target</code>.</p><p>It is important to ensure that the values you set are within the safe movement range of [0, 5200] to protect the sliding platform on the robot and ensure it operates safely within this range.</p>',25)])])}const Q=c(K,[["render",U]]);export{Z as __pageData,Q as default};
